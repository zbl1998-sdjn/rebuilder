# ProgramBench Cleanroom Goal Audit

Date: 2026-05-14

## 2026-05-16 Active Goal Audit Addendum

Active objective: achieve breakthroughs across the official ProgramBench set
while avoiding test-set overfitting and improving external generalization.

Concrete completion criteria for this objective:

1. Every tracked official-baseline task has either a verified higher official
   aggregate score or a documented, current blocker.
2. No candidate reaches packaging or official evaluation unless aggregate local
   holdout, holdout-improvement, smoke-axis, and generalization-risk gates pass.
3. Strategy skills remain cleanroom domain guidance, not test-set answer tables.
4. The framework has fresh verification proving the gates and prompt/schema
   changes are executable.

Fresh evidence from 2026-05-16:

| Requirement | Evidence | Status |
| --- | --- | --- |
| Strict official-eligible candidates exist before eval | `python scripts/rank_programbench_candidates.py --runs runs --official-eval-root runs\programbench_official_eval --baseline-root baselines\programbench --official-eligible-only --latest-per-task --min-smoke-contract-axes 1 --require-holdout-improvement --min-holdout-improvement-delta 0.02 --limit 20` printed only the markdown header and no rows. The same strict gate with `--format json` emitted `schema_version=1`, `row_count=0`, `total_row_count=0`, and `rows=[]`. | Missing |
| Strict candidate ranking output is machine-readable without hidden details | `rank_programbench_candidates.py --latest-per-task --limit 3 --format json` emitted aggregate rows with local/holdout rates, holdout cases, smoke/adaptive axis counts, gate reason, status, aggregate counts, official marker, and result path only. Regression coverage adds a hidden marker to local failure payloads and verifies it is absent from stdout. | Satisfied |
| Generalization risk is low enough for broad official progress | `python scripts/audit_generalization_risk.py --runs runs --baseline-root baselines\programbench --official-eval-root runs\programbench_official_eval --fail-on-risk high --limit 20` exited `1`; high-risk blockers remain for missing reliable holdout, latest holdout regression, and new-axis expansion regression | Missing |
| Generalization risk output is machine-readable without hidden details | `python scripts/audit_generalization_risk.py --runs runs --baseline-root baselines\programbench --official-eval-root runs\programbench_official_eval --limit 12 --format json` emitted `schema_version=1`, `row_count=12`, and `total_row_count=12`. `--fail-on-risk high --format json` still returned non-zero while printing parseable JSON containing only official aggregates, target class, risk level/reason, holdout aggregates, result paths, and required next action. | Satisfied |
| Ready baseline-gate tasks are identified without hidden details | The same risk audit reports `clog-cli` and `nnn` as low risk / latest reliable gate pass, but the strict official-eligible table with holdout-improvement requirements is still empty | Partial |
| Official breakthrough queue is machine-readable without hidden details | `plan_official_breakthrough_targets.py --include-next-command --baseline-upgrade-min-smoke-contract-axes 1 --baseline-upgrade-require-holdout-improvement --baseline-upgrade-min-holdout-improvement-delta 0.02 --include-restore-ablation-command --include-missing-holdout-command --limit 12 --format json` emitted `schema_version=1`, `row_count=12`, and `total_row_count=12`; rows cover `ready_baseline_gate`, `restore_historical_gate`, `weak_cleanroom_rerun`, and `missing_reliable_holdout`, with only official aggregate counts, local holdout aggregates, result paths, and guarded dry-run next commands. Ready-row `next_command` values now include the strict smoke-axis and holdout-improvement gates used by the final official-eligible audit. | Satisfied |
| Restore targets have aggregate-only next actions | `plan_official_breakthrough_targets.py --include-next-command --include-restore-ablation-command ... --limit 12` lists restore ablation dry-run commands for `htmlq`, `go-mod-outdated`, `gron`, `zip-password-finder`, `xsv`, `csview`, and `cmatrix`, all with `--skip-official-eval`, `--require-holdout-improvement`, `--max-generalization-risk low`, and smoke-axis gates. `run_restore_axis_ablation_batch.py --axis-action-domain csv_table --show-axis-action --limit 7` uses the cleanroom restore audit action to focus the batch; fresh `--format json` dry-run selected only `burntsushi__xsv.f430466`, reported `row_count=1`, and emitted a command array containing `--skip-official-eval` and `--dry-run`. | Satisfied |
| Missing reliable holdout rows have local-only next actions | `run_missing_holdout_cleanroom_rerun.py` defaults to dry-run, requires `--execute --ack-external-llm-docker` for real execution, always forwards `--skip-official-eval`, and intentionally omits `--require-holdout-improvement` until reliable local signal exists. Fresh planner JSON smoke emitted missing-holdout commands for `alecthomas__chroma.8d04def` and `rbakbashev__elfcat.52f8cc7` with `--dry-run` and `--min-smoke-contract-axes 1`; focused wrapper/planner tests reported `13 passed`. | Satisfied |
| Restore regressions expose cleanroom-local axis deltas | `scripts/audit_restore_targets.py --runs runs --baseline-root baselines\programbench --limit 7` now includes `added axes` / `removed axes` plus an `axis action` column derived only from `implementation_metadata.probe_axis_coverage`; `--format json` emits the same cleanroom-safe payload for batch consumers. Fresh JSON smoke with `--limit 2 --format json` reported `row_count=2` and `total_row_count=7`. Current restore rows recommend `ablate_added_axis_domains:html_selector` for `htmlq`, `json_transform` for `gron`, `archive_compression,json_transform` for `zip-password-finder`, and `csv_table` for `xsv`; same-axis regressions such as `go-mod-outdated`, `csview`, and `cmatrix` are marked `inspect_same_axis_strategy_regression`. | Satisfied |
| External LLM/Docker execution requires explicit acknowledgement | `run_official_closed_loop.py` rejects direct runs without `--ack-external-llm-docker` before loading samples or running commands; non-dry-run `run_official_strategy_ablation.py` requires the same acknowledgement and forwards it to child closed-loop commands; restore, weak-task, weak-task batch, and missing-holdout wrappers forward it when `--execute` is authorized; `run_programbench_mini_lab.py` requires the same acknowledgement before loading the catalog or preparing/running tasks. Focused regression tests for these entrypoints include ack-blocking paths; negative CLI smoke exits before external execution. | Satisfied |
| Strategy skills preserve cleanroom philosophy | `python scripts/audit_strategy_domain_coverage.py --fail-on-missing` shows all 11 domains `ok`, each with `generalization items=3`, `validation items=3`, and `cleanroom issues=0`. `--format json` emitted `schema_version=1`, `row_count=11`, and `total_row_count=11` with only per-domain aggregate counts/status, not strategy text or matched policy snippets. | Satisfied |
| File-mode behavior contracts stay cleanroom-local | `BehaviorContract` now preserves safe relative `input_files` and input previews from cleanroom observations; implementation prompts show the file names and bounded previews, while Python runtime smoke writes those files into a temporary workdir for safe contract CLI cases. Spec synthesis drops unsafe input-file names/previews from observation prompts and redacts matching unsafe argv values; file-like argv values that are absolute, drive-qualified, or contain parent traversal are redacted even when they did not originate from `input_files`. Runtime smoke skips unsafe file-path argv cases even when safe input files are present. Regression tests first failed on missing fields / missing prompt payload / skipped file-input smoke / unsafe path execution / raw unsafe argv leakage, then passed after implementation. | Satisfied |
| Environment-sensitive behavior contracts stay cleanroom-local | `BehaviorContract` now preserves safe environment variables from cleanroom observations; spec prompts and implementation behavior contracts include only valid short names/values, while filtering sensitive token/secret/password/key/credential/auth names. Python runtime smoke forwards the filtered env vars so terminal UI/animation dispatch can be checked without carrying credentials, and smoke de-duplication now includes argv, stdin, input-file content fingerprints, and env vars. Regression tests first failed on missing env prompt payload, skipped env runtime smoke, and env-only contract de-duplication, then passed after implementation. | Satisfied |
| Runtime-smoke input dimensions are auditable without raw payloads | `PythonRuntimeSmokeChecker.check()` returns a `RuntimeSmokeReport` while preserving the existing `find_issues()` API; `ImplementerAgent` records `generation_metadata["runtime_smoke"]` with status, case counts, contract-case counts, and aggregate input dimensions actually executed during implementation smoke. The payload omits argv values, file contents, stdout, stderr, holdout failures, and official hidden-test details. Regression first failed on missing metadata, then passed after implementation. | Satisfied |
| Historical candidates can be audited for runtime-smoke replay readiness | `scripts/audit_runtime_smoke_replay.py` scans historical `result.json` artifacts, reconstructs safe behavior contracts from local evidence stores, and defaults to read-only aggregate reporting. A real read-only scan with `--require-runtime-smoke-dimensions args,input_files --format json --limit 200` found 96 `insufficient_contract_artifacts`, 16 `ready_for_replay`, and 4 `missing_entrypoint` rows. A local-only `--execute` scan found 14 `replay_passed`, 2 `replay_failed`, 96 `insufficient_contract_artifacts`, and 4 `missing_entrypoint` rows; the failed rows were `chmln__sd.87d1ba5` (`runtime_smoke_traceback`) and `sheepla__pingu.926d475` (`runtime_smoke_timeout`). JSON output is aggregate-only and omits argv values, file contents, stdout/stderr, holdout failures, and official hidden details. | Satisfied |
| Strict-gate runtime-smoke blockers can be cross-audited against replay | `scripts/audit_runtime_smoke_gate_replay.py` joins latest candidate gate blockers with local runtime-smoke replay status without mutating historical `result.json`. A real strict scan with `--latest-per-task --min-smoke-contract-axes 1 --require-runtime-smoke-dimensions args,input_files --require-holdout-improvement --min-holdout-improvement-delta 0.02 --execute-replay --format json --limit 100` emitted 23 aggregate rows: 0 `metadata_only_runtime_smoke_blocker`, 6 `replay_resolved_but_other_blockers_remain`, and 17 `replay_failed_or_incomplete`. This proves the empty strict official-eligible table is not merely a missing runtime-smoke metadata artifact; remaining blockers include low holdout, missing improvement, too few cases, smoke-axis gaps, and existing official/baseline status. JSON output omits raw argv, input contents, stdout/stderr, holdout failures, and official hidden details. | Satisfied |
| Official candidate ranking can require runtime-smoke dimensions | `scripts/rank_programbench_candidates.py` now accepts `--require-runtime-smoke-dimensions args,input_files,...`; the optional gate requires `runtime_smoke.status == "passed"` and the requested aggregate input dimensions before a row can be official-eligible. It emits only status/counts/dimension names in JSON/markdown and rejects rows with missing dimensions or failed smoke without inspecting argv, file contents, stdout/stderr, holdout failures, or official hidden-test details. Regression first failed on the missing `required_runtime_smoke_dimensions` argument, then passed after implementation. | Satisfied |
| Official candidate ranking exposes all aggregate blockers | `scripts/rank_programbench_candidates.py` now keeps the existing first `official_gate` reason and also emits `official_gate_blockers` in JSON so earlier gates do not hide later blockers such as missing runtime-smoke metadata. Regression first failed on missing `official_gate_blockers`, then passed. A real JSON smoke over latest-per-task rows with strict smoke-axis, runtime-smoke dimensions, and holdout-improvement gates emitted `row_count=23`, `total_row_count=23`, and blocker counts including `runtime_smoke_not_passed=23`, `low_holdout_rate=17`, `insufficient_smoke_contract_axes=17`, `already_official=12`, and `too_few_holdout_cases=10`, all from aggregate local metadata. | Satisfied |
| Breakthrough planner forwards runtime-smoke dimension gates | `scripts/plan_official_breakthrough_targets.py` now accepts `--baseline-upgrade-require-runtime-smoke-dimensions args,input_files` and threads it only into ready-baseline `rank_programbench_candidates.py` next commands. Fresh planner JSON dry-run with `--include-next-command`, strict smoke-axis, holdout-improvement, restore, and missing-holdout flags emitted `schema_version=1`, `row_count=12`, and `total_row_count=12`; ready-baseline rows included `--require-runtime-smoke-dimensions 'args,input_files'`, while restore/weak/missing rows remained guarded dry-run commands. | Satisfied |
| Packaging and closed-loop entrypoints can require runtime-smoke dimensions | `core.submission.SubmissionHoldoutGate`, `scripts/package_submission.py`, and `scripts/run_official_closed_loop.py` now accept the same aggregate `args,input_files,...` runtime-smoke dimension gate. The direct closed-loop gate checks `runtime_smoke.status == "passed"` and required dimensions before packaging or official eval; wrapper commands for strategy ablation, restore-axis batch, weak-task rerun, weak-task batch, and missing-holdout rerun forward the same flag into their guarded child commands. Regression tests first failed on missing constructor/CLI args and missing child-command flags, then passed after implementation. Fresh wrapper dry-run smokes showed the flag in strategy, weak, missing-holdout, restore-batch JSON, and weak-batch JSON commands without running external LLM, Docker, or official eval. Latest full `.venv` pytest reported `722 passed in 12.29s`; strict official-eligible JSON gate with runtime-smoke dimensions still reported `row_count=0` and `total_row_count=0`. | Satisfied |
| Repair no longer accepts syntax-broken candidates | `core/meta_controller.py` rejects repair candidates with integrity/runtime-smoke issues as `repair_integrity_failed`; regression covered by `tests/test_meta_controller_config.py::test_meta_controller_rejects_syntax_broken_repair_before_acceptance` | Satisfied |
| Full local verification is fresh | `tests/test_audit_generalization_risk.py` reported `4 passed`, `tests/test_audit_strategy_domain_coverage.py` reported `4 passed`, `tests/test_rank_programbench_candidates.py` reported `58 passed`, `tests/test_summarize_holdout_trends.py` reported `31 passed`, `tests/test_run_weak_task_cleanroom_rerun_batch.py` reported `7 passed`, the new file-input contract/runtime-smoke focused tests first failed then passed, unsafe input-file prompt/runtime-smoke tests first failed then passed, raw unsafe argv redaction first failed then passed, env-var contract/runtime-smoke tests first failed then passed, env-only runtime-smoke de-duplication first failed then passed, runtime-smoke metadata first failed on missing `generation_metadata["runtime_smoke"]` then passed, runtime-smoke dimension gate first failed on missing `required_runtime_smoke_dimensions` then passed, breakthrough planner pass-through first failed on an unrecognized CLI flag then passed, related implementer/integrity tests reported `55 passed`, related spec/prompt/integrity tests reported `65 passed`, `tests/test_plan_official_breakthrough_targets.py` reported `8 passed`, ruff passed for all JSON/batch and file/env-smoke touched file pairs plus `core\codebase\runtime_smoke.py`, `core\implementer_agent.py`, `scripts\rank_programbench_candidates.py`, `scripts\plan_official_breakthrough_targets.py`, `tests\test_agent_output_parsing.py`, `tests\test_rank_programbench_candidates.py`, and `tests\test_plan_official_breakthrough_targets.py`, real generalization-risk JSON smoke emitted `row_count=12` and `total_row_count=12`, real strategy-coverage JSON smoke emitted `row_count=11` and `total_row_count=11`, real strict official-eligible JSON smoke emitted `row_count=0` and `total_row_count=0`, real strict official-eligible JSON smoke with `--require-runtime-smoke-dimensions args,input_files` also emitted `row_count=0` and `total_row_count=0`, real weak-task trend/recommendation JSON emitted `row_count=3`, `total_row_count=13`, recommendation `row_count=3`, and recommendation `total_row_count=4`, real weak-task batch JSON dry-run emitted `row_count=3` and top targets `hexyl`, `zoxide`, `pingu`, planner/wrapper tests passed (`14 passed`), real missing-holdout wrapper dry-run printed a child command with `--skip-official-eval`, real planner JSON smoke emitted `row_count=12` including missing-holdout commands and strict ready baseline-upgrade commands, real planner JSON smoke with `--baseline-upgrade-require-runtime-smoke-dimensions args,input_files` emitted `row_count=12` and ready-baseline next commands containing `--require-runtime-smoke-dimensions 'args,input_files'`, real restore-batch JSON smoke emitted `row_count=1`, strategy coverage gate passed with 11 domains `ok`, strict official-eligible output remained empty, `audit_generalization_risk.py --fail-on-risk high` still exited `1`, runtime-smoke replay audit tests reported `4 passed`, gate/replay cross-audit tests reported `3 passed`, related runtime-smoke/ranking/gate tests reported `66 passed`, and latest full `.venv` pytest reported `722 passed in 12.29s` | Satisfied |

Completion decision: do not mark the active goal complete. The current blockers
are real: strict official-eligible output is empty, broad generalization risk is
still high, and no authorized external LLM/Docker restore ablation or weak-task
rerun has produced a new local holdout improvement or official aggregate
breakthrough in this continuation.

2026-05-16 note: an attempted `sharkdp__hexyl.2e26437`
`run_weak_task_cleanroom_rerun.py --execute --ack-external-llm-docker`
command with `--skip-official-eval` semantics was rejected by the environment
policy because it would send task/code context to an external LLM service.
Do not work around that rejection; continue with local aggregate audits and
dry-run planning unless policy allows a safer external execution path.

Next concrete action after explicit authorization is to execute one guarded
restore-axis ablation, starting with `xsv` or `htmlq`, using the dry-run command
shape emitted by `plan_official_breakthrough_targets.py`. Without that
authorization, continue only local aggregate audits, prompt/gate hardening, and
dry-runs.

## Objective

Continue improving ReBuilder under ProgramBench cleanroom constraints:

1. Keep official eval guarded by aggregate local gates.
2. Improve candidate ranking and strategy selection.
3. Drive at least one weak task to a verified local holdout improvement.
4. Invoke official eval only when the official-eligible gate passes.

## Evidence Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Official eval guard exists | `scripts/audit_official_eval_gate.py`; `scripts/rank_programbench_candidates.py --official-eligible-only --latest-per-task`; closed-loop packaging/eval gate in `scripts/run_official_closed_loop.py`; optional official-audit, ranking, and packaging holdout improvement gates | Satisfied |
| Official audit gate thresholds must be bounded and finite | `scripts/audit_official_eval_gate.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, plus negative, non-finite, or fractional integer gates, both through the CLI and direct function calls, preventing manual official-readiness audits from using impossible aggregate local gates | Satisfied |
| Official candidate-ranking gate thresholds must be bounded and finite | `scripts/rank_programbench_candidates.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, plus negative, non-finite, or fractional integer gates, at the CLI boundary, candidate collection boundary, and official gate reason boundary, preventing aggregate ranking and `--official-eligible-only` output from using impossible local gates | Satisfied |
| Official candidate ranking is deterministic | `scripts/rank_programbench_candidates.py` sorts candidate rows by aggregate ranking metrics and then task id, so equal-score rows do not inherit filesystem traversal order | Satisfied |
| Recorded-baseline discovery is robust | `scripts/rank_programbench_candidates.py` ignores malformed non-object `.baseline.json` payloads, so recorded baseline artifacts do not crash official candidate ranking or readiness audits | Satisfied |
| Official candidate same-task result selection is deterministic | `scripts/rank_programbench_candidates.py` breaks identical same-task candidate selection ties by result path for both aggregate ranking and `--latest-per-task` selection | Satisfied |
| Official candidate-ranking display limit must be positive | `scripts/rank_programbench_candidates.py` rejects non-positive `--limit` values before printing markdown, preventing `--limit 0` or negative limits from producing misleading empty official-candidate tables | Satisfied |
| Official candidate metadata parsing is robust | `scripts/rank_programbench_candidates.py` treats malformed `implementation_metadata` or `probe_axis_coverage` as empty metadata, so candidate ranking and official gate audits do not crash on malformed aggregate metadata | Satisfied |
| Official candidate aggregate numeric parsing is robust | `scripts/rank_programbench_candidates.py` treats malformed aggregate numeric fields such as `holdout_cases`, `probes_conducted`, and `iterations_used` as zero, treats negative or fractional aggregate count fields as zero, treats non-finite or out-of-range plain rates as zero, and treats non-finite or out-of-range holdout rates as missing, so malformed local `result.json` files fail gates instead of crashing or bypassing candidate ranking and audit tools | Satisfied |
| Aggregate result readers ignore non-object payloads | `scripts/rank_programbench_candidates.py` and `scripts/summarize_holdout_trends.py` ignore syntactically valid JSON payloads that are not objects, so malformed files under `runs` do not crash candidate ranking, weak-task recommendations, or holdout-improvement audits | Satisfied |
| Official closed-loop result parsing is robust | `scripts/run_official_closed_loop.py` treats unreadable, syntactically invalid, non-object, malformed aggregate numeric, negative/fractional aggregate counts, and non-finite or out-of-range holdout-rate local `result.json` payloads as missing or zeroed holdout evidence, so the path exits through local holdout gates before packaging or official eval | Satisfied |
| Mini-lab aggregate result collection is robust | `core/experiments/mini_lab.py` treats malformed, non-finite, or out-of-range aggregate rate fields, malformed/fractional/negative count fields, malformed metadata, syntactically invalid JSON, and non-object local `result.json` payloads as missing evidence; invalid payloads become aggregate-only `invalid_result` rows so batch summaries continue | Satisfied |
| Baseline local-result recording is robust | `core/experiments/baseline.py` treats malformed, non-finite, or out-of-range local aggregate rate fields, malformed/fractional/negative count fields, and unreadable or invalid local `result.json` payloads as missing evidence, so official aggregate baseline records can still be written without storing malformed local values | Satisfied |
| Official eval aggregate parsing is robust | `core/evaluation/programbench.py` treats malformed official result items as aggregate failures, filters malformed entries out of branch-specific summaries, normalizes malformed warnings to empty, and converts unreadable or invalid official eval JSON into a zero-test `invalid_eval_payload` summary | Satisfied |
| Run pruning aggregate readers are robust | `scripts/prune_runs.py` ignores non-object baseline and local `result.json` payloads, so malformed aggregate artifacts do not crash dry-run/apply pruning reports or valid baseline path protection | Satisfied |
| Aggregate experiment registry loading is robust | `core/experiments/registry.py` skips syntactically invalid JSONL rows and non-object rows while still rejecting object rows with hidden per-test details, and validates official aggregate `score`/`pass_rate` as finite rates between `0` and `1`, preserving strategy selection availability, deterministic bandit scoring, and cleanroom boundaries | Satisfied |
| Aggregate experiment registry counts are consistent | `core/experiments/registry.py` rejects negative official aggregate `passed_tests`, `total_tests`, and `warning_count` values, and rejects `passed_tests > total_tests`, so strategy history cannot store malformed official aggregate counts | Satisfied |
| Aggregate experiment registry holdout cases are non-negative | `core/experiments/registry.py` rejects negative local aggregate `holdout_cases` both when constructing `ExperimentRun` rows and when loading registry JSONL, so strategy history cannot store malformed local holdout evidence counts | Satisfied |
| Strategy registry numeric params are bounded | `core/experiments/registry.py` rejects negative numeric strategy parameters for probe/repair/token/timeout/temperature knobs and enforces `min_coverage`/`top_p` as finite rates between `0` and `1`, so learned strategy history cannot store weakened or malformed aggregate-only run controls | Satisfied |
| ProgramBench sample metadata fetch parsing is robust | `core/programbench/samples.py` validates that DockerHub public metadata responses contain a JSON list of repository results and skips malformed individual repository rows, so catalog generation can keep valid public metadata without touching hidden tests | Satisfied |
| ProgramBench sample metadata skips malformed repository names | `core/programbench/samples.py` rejects non-string or empty DockerHub repository names as malformed rows, preventing public metadata glitches from crashing sample catalog generation before valid rows are processed | Satisfied |
| ProgramBench sample fetch CLI rejects empty successful catalogs | `scripts/fetch_programbench_samples.py` exits before writing an output file when a positive `--limit` produces zero valid public metadata records, so downstream cleanroom batch entrypoints do not consume a success-shaped empty catalog | Satisfied |
| ProgramBench sample fetch CLI rejects non-positive limits | `scripts/fetch_programbench_samples.py` validates `--limit` as a positive integer before fetching or writing, so setup commands cannot intentionally or accidentally generate an empty catalog through `--limit 0` | Satisfied |
| ProgramBench sample catalog loading is robust | `core/programbench/catalog.py` requires the catalog payload to be a JSON list and skips malformed individual sample entries, so cleanroom batch selection and official closed-loop entrypoints can keep using valid catalog rows while bad file shapes fail clearly | Satisfied |
| ProgramBench sample catalog rejects invalid JSON clearly | `core/programbench/catalog.py` converts syntactically invalid catalog JSON into a clear `ValueError`, so cleanroom batch and official closed-loop entrypoints fail at the catalog boundary instead of leaking JSON parser text | Satisfied |
| ProgramBench sample catalog rejects duplicate instance ids | `core/programbench/catalog.py` raises a clear `ValueError` for duplicate valid `instance_id` rows, so cleanroom batch and official closed-loop sample selection does not silently depend on catalog row order | Satisfied |
| Weak-task improvement gate exists | `scripts/audit_holdout_improvement.py`; `run_official_closed_loop.py --require-holdout-improvement`; `scripts/package_submission.py --require-holdout-improvement`; ablation and weak-task wrappers pass the same gate; `--min-holdout-improvement-delta` can require a positive margin over the previous reliable best, and below-threshold gains report `delta_below_min` | Satisfied |
| Holdout improvement gate thresholds must be finite and non-negative | `scripts/audit_holdout_improvement.py` rejects negative or non-finite `min_delta` and negative, non-finite, or fractional `min_holdout_cases` both through the CLI and direct function calls, preventing official gate, ranking, packaging, and closed-loop callers from weakening the aggregate improvement check | Satisfied |
| Holdout improvement previous-best evidence is deterministic | `scripts/audit_holdout_improvement.py` breaks identical previous-best aggregate ties by result path, so official audit improvement evidence does not inherit filesystem traversal order | Satisfied |
| Strategy selection uses aggregate-only feedback | `core/experiments/registry.py`, `core/experiments/bandit.py`, and closed-loop strategy registry tests | Satisfied |
| Strategy selection holdout-case threshold must be integral | `core/experiments/bandit.py` rejects negative, non-finite, or fractional `min_holdout_cases` before filtering aggregate registry history, preventing direct callers from weakening strategy selection evidence requirements | Satisfied |
| Strategy ablation preserves cleanroom boundary | `scripts/run_official_strategy_ablation.py` treats holdout gate exit `3` as local skip and can pass `--require-holdout-improvement` | Satisfied |
| Strategy ablation gate thresholds must be bounded and finite | `scripts/run_official_strategy_ablation.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, negative integer gates, negative probe/repair/retry counts, non-positive worker/CPU counts, and non-positive or non-finite command timeouts before constructing closed-loop commands, preventing batch ablations under weakened aggregate gates or invalid execution controls | Satisfied |
| Repair target prompts and exclusion keys are deterministic for the same failure set | `core/repair/clustering.py` sorts reports inside each `FailureCluster` by stable test-case fields, while `target_key()` uses test-case name, input fingerprint, and description fingerprint; `core/meta_controller.py` hashes the structured key for aggregate-safe telemetry; `tests/test_failure_clustering.py` covers reversed input order and same-name/different-args keys | Satisfied |
| Repair target exclusion keys distinguish input payloads | `core/repair/clustering.py` uses `test_case_fingerprint()` to distinguish args, stdin, input-file, and environment differences in the structured repair target key before hashing, so regressive repair exclusion does not skip same-name failures with different local input payloads | Satisfied |
| Repair target keys avoid raw input payload values | `core/repair/clustering.py` uses `test_case_fingerprint()` for input payload identity and hashes test-case descriptions, so structured repair exclusion keys distinguish local inputs without carrying raw args, stdin, environment variable values, or raw description text before metadata hashing | Satisfied |
| Repair prompt file ordering is deterministic | `core/repair_loop.py` sorts existing codebase file names in repair diagnosis prompts and sorts target file context before repair application, so equivalent repair inputs do not change LLM prompt ordering because of dict or target-file list order | Satisfied |
| Repair strategy target files are normalized | `core/repair_loop.py` accepts a single target-file string, drops empty or non-string target-file entries, and deduplicates/sorts target files before constructing `RepairStrategy`, so malformed repair strategy JSON does not crash parsing or create unstable target ordering | Satisfied |
| Repair strategy text fields are normalized | `core/repair_loop.py` defaults malformed or empty `strategy_type` values to `fix_algorithm` and serializes non-string `description` values to stable JSON text before constructing `RepairStrategy`, so malformed repair strategy JSON does not crash parsing | Satisfied |
| Targeted repair merges only target files | `core/repair_loop.py` ignores LLM-returned non-target files when `strategy.target_files` is non-empty, preserving existing files and avoiding unrelated additions during targeted repair; when no target files are specified, the existing full-merge fallback remains | Satisfied |
| Targeted repair preserves build metadata | `core/repair_loop.py` preserves the existing `build_script` and `executable_path` when `strategy.target_files` is non-empty, so targeted repair cannot silently redirect execution through LLM-returned build metadata while still allowing the existing full-merge fallback when no target files are specified | Satisfied |
| Targeted repair normalizes direct target files | `core/repair_loop.py` normalizes `RepairStrategy.target_files` inside `apply_repair`, so direct callers get the same trim, dedupe, and stable-order behavior as LLM JSON parsing before prompt context selection, file merging, and targeted build-metadata preservation | Satisfied |
| Targeted repair normalizes target file paths | `core/repair_loop.py` reuses `normalize_output_path()` for repair target files, so LLM JSON and direct callers normalize backslashes, `./` prefixes, duplicates, and unsafe absolute or parent-traversal paths before prompt context selection, returned-file merging, and targeted build-metadata preservation | Satisfied |
| Weak-task rerun recommendations are aggregate-only | `scripts/summarize_holdout_trends.py --recommend-weak-reruns --include-rerun-command` reads aggregate `result.json` holdout fields and emits guarded local rerun commands; `--rerun-min-smoke-contract-axes` and `--rerun-min-holdout-improvement-delta` can carry optional local gates into those commands. `--format json` emits the same trend/recommendation queue as aggregate-only JSON with guarded dry-run commands, not raw failure payloads. | Satisfied |
| Weak-task rerun recommendation gate thresholds must be bounded and finite | `scripts/summarize_holdout_trends.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, plus negative or fractional smoke-axis gates, both through the CLI and direct helper calls before rendering guarded rerun commands, so recommendation output cannot suggest impossible or weakened local gates | Satisfied |
| Weak-task trend ordering is deterministic | `scripts/summarize_holdout_trends.py` sorts trend rows by aggregate rates/case counts and then task id, and breaks same-task best/latest ties by result path, so equal-score rows do not inherit filesystem traversal order | Satisfied |
| Weak-task trend display limit must be positive | `scripts/summarize_holdout_trends.py` rejects non-positive `--limit` values before printing trend or rerun recommendation tables, preventing `--limit 0` or negative limits from hiding weak-task recommendations | Satisfied |
| Weak-task trend numeric parsing is robust | `scripts/summarize_holdout_trends.py` treats malformed, negative, non-finite, fractional, or boolean `holdout_cases` values as zero, treats non-finite or out-of-range plain rates as zero, and treats non-finite or out-of-range holdout rates as missing, so malformed local history rows are filtered instead of crashing or bypassing weak-task recommendations and holdout-improvement audits | Satisfied |
| Weak-task local-only rerun entrypoint exists | `scripts/run_weak_task_cleanroom_rerun.py` defaults to dry-run, requires explicit `--execute`, always adds `--skip-official-eval --require-holdout-improvement`, and does not expose official eval run-name/root options. `scripts/run_weak_task_cleanroom_rerun_batch.py` selects aggregate-only weak-task recommendations, defaults to dry-run, emits text/JSON command plans, and requires `--execute --ack-external-llm-docker` for real child execution. | Satisfied |
| Weak-task local-only rerun gate thresholds must be bounded and finite | `scripts/run_weak_task_cleanroom_rerun.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, negative integer gates, negative probe/repair/retry counts, non-positive worker/CPU counts, and non-positive or non-finite command timeouts during argument parsing, preventing accidental gate weakening or invalid execution controls before an authorized local rerun | Satisfied |
| Official closed-loop gate thresholds must be bounded and finite | `scripts/run_official_closed_loop.py` rejects holdout-rate thresholds outside finite `0..1`, negative or non-finite holdout-improvement thresholds, negative integer gates, negative probe/repair/retry counts, and non-positive worker/CPU counts during argument parsing, preventing automated runs from preparing, packaging, or evaluating under weakened aggregate gates or invalid execution controls | Satisfied |
| Per-domain smoke contract coverage exists | `core/probing/adaptive.py` emits local-only `smoke_contract:<domain>.<axis>` probes for `json_transform`, `html_selector`, `csv_table`, `network_ping`, `archive_compression`, and `binary_hexdump`; `core/probe_engine.py` preserves those axes as probe sample tags; `core/probing/corpus.py` treats these axes as holdout dimensions; `core/probing/axis_coverage.py` records aggregate-safe axis counts/domains in result metadata | Satisfied |
| Holdout split preserves budget while covering dimensions | `core/probing/corpus.py` keeps stateful plans atomic, prioritizes behavior/smoke dimensions, and now prefers smaller equal-coverage atomic groups that fit the remaining holdout budget, reducing exploration starvation from oversized groups | Satisfied |
| Official gate can require smoke-axis coverage and local improvement | `scripts/rank_programbench_candidates.py`, `scripts/audit_official_eval_gate.py`, `scripts/package_submission.py`, `scripts/run_official_closed_loop.py`, `scripts/run_official_strategy_ablation.py`, and `scripts/run_weak_task_cleanroom_rerun.py` expose or pass an optional `--min-smoke-contract-axes` gate. `rank_programbench_candidates.py`, `audit_official_eval_gate.py`, and `package_submission.py` can also require aggregate local holdout improvement with `--require-holdout-improvement --min-holdout-improvement-delta`. Defaults remain unchanged. | Satisfied |
| Official audit output includes aggregate improvement evidence | `scripts/audit_official_eval_gate.py --require-holdout-improvement` reports the previous reliable best holdout rate, case count, result path, and delta used for the decision; it does not include holdout failure details | Satisfied |
| Official audit rejects invalid local results with structured JSON | `scripts/audit_official_eval_gate.py` returns `eligible=false` and `reason=invalid_result` for malformed local `result.json` payloads instead of emitting stack traces or detailed failure data | Satisfied |
| Manual packaging gate rejects malformed aggregate payloads predictably | `core/submission/gate.py` rejects malformed or non-finite aggregate holdout values, out-of-range holdout rates, fractional or malformed holdout/smoke-axis counts, non-object payloads, and syntactically invalid `result.json` files with `HoldoutGateError`, keeping package creation on explicit aggregate gate failures | Satisfied |
| Manual packaging gate thresholds must be bounded and integral | `core/submission/gate.py` rejects negative, non-finite, or greater-than-one holdout-rate thresholds and rejects negative or fractional integer gates; `scripts/package_submission.py` rejects invalid holdout-rate thresholds before archive creation, preventing manual submissions under down-clamped or truncated local gates | Satisfied |
| Submission archives exclude local tooling debris | `core/submission/packager.py` excludes `.git`, Python/cache directories, virtual environments, `node_modules`, and stale `submission.tar.gz` files from official-eval submission archives | Satisfied |
| Submission archives are reproducible for identical content | `core/submission/packager.py` writes gzip archives with fixed header metadata and normalizes tar member mtime, ownership, names, and modes, so identical generated sources produce byte-identical submission archives independent of filesystem metadata | Satisfied |
| Current official-eligible set is checked before official eval | Latest `rank_programbench_candidates.py --official-eligible-only --latest-per-task` output is an empty table, including with smoke-axis and holdout-improvement gates enabled; `--format json` reports the same strict state as `row_count=0`, `total_row_count=0`, and `rows=[]` | Satisfied |
| At least one weak task has verified improved local holdout | No post-change authorized rerun has completed. Latest weak-task JSON queue still recommends top dry-run targets `hexyl`, `zoxide`, and `pingu`; none has a new improved aggregate holdout after the smoke-contract change. | Missing |
| Official eval invoked only when eligible | No official eval was invoked after the latest gate checks; eligible table is empty | Satisfied |

## Current Weak-Task Queue

The aggregate-only recommendation command is:

```powershell
python scripts\summarize_holdout_trends.py `
  --runs runs `
  --limit 3 `
  --min-holdout-cases 10 `
  --recommend-weak-reruns `
  --include-rerun-command `
  --rerun-root runs\weak_task_cleanroom_next `
  --rerun-min-smoke-contract-axes 1 `
  --rerun-min-holdout-improvement-delta 0.02
```

Current top recommendations from local aggregate trends:

1. `sharkdp__hexyl.2e26437`: target `>=80.0% and >20.0%`
2. `ajeetdsouza__zoxide.67ca1bc`: target `>=80.0% and >31.7%`
3. `sheepla__pingu.926d475`: target `>=80.0% and >58.3%`

The preferred next command after explicit authorization is:

```powershell
python scripts\run_weak_task_cleanroom_rerun.py sharkdp__hexyl.2e26437 `
  --runs runs\weak_task_cleanroom_next\sharkdp__hexyl.2e26437 `
  --min-smoke-contract-axes 1 `
  --min-holdout-improvement-delta 0.02 `
  --pull `
  --execute
```

This wrapper keeps official eval disabled. It may call external LLM APIs and
Docker when `--execute` is present, so it requires explicit user authorization
before execution. Without `--execute`, it only prints the guarded closed-loop
command.

## Verification Snapshot

- Focused smoke contract tests: `15 passed`
- Holdout split, probe-axis, probe-engine, and meta-controller tests: `36 passed`
- Official audit/ranking improvement evidence tests: `24 passed`
- Candidate ranking, official audit, packaging gate, closed-loop, and holdout-improvement audit tests: `91 passed`
- Submission packaging, holdout gate, eval script, official closed-loop, and holdout-improvement audit tests: `70 passed`
- Official audit/ranking, packaging, closed-loop, strategy-ablation, weak-rerun, holdout trend, and holdout-improvement audit tests: `186 passed`
- Candidate ranking, trend/improvement, official audit, packaging, closed-loop, baseline, mini-lab, registry, and strategy aggregate tests: `227 passed`
- Weak-rerun helper boundary, local-only rerun, and holdout-improvement audit tests: `39 passed`
- Baseline recorder, official closed-loop, candidate ranking, and official audit tests: `57 passed`
- Official eval parser and dependent baseline/closed-loop/experiment tests: `56 passed`
- ProgramBench metadata, catalog, adapter, mini-lab selection, and closed-loop entrypoint tests: `56 passed`
- Run pruning tests: `6 passed`
- Aggregate registry, strategy bandit, and closed-loop strategy tests: `88 passed`
- Mini-lab and aggregate experiment tests: `44 passed`
- Official-gate smoke-axis path tests: `66 passed`
- Holdout improvement audit tests: `7 passed`
- Repair prompt, failure clustering, and meta-controller repair tests: `44 passed`
- Baseline recorder and mini-lab aggregate parser tests: `14 passed`
- Official closed-loop, strategy-ablation, and weak-rerun parser tests: `102 passed`
- Official audit, candidate-ranking, and weak-trend bounded rate/limit tests: `109 passed`
- Full local test suite: `604 passed`
- `compileall` passed for `main.py`, `core`, `llm_clients`, and `scripts`
- `ruff` passed for the smoke-contract, corpus-split, submission, official-gate, holdout-improvement, ProgramBench catalog/sample-fetch, repair-target, repair target-key sanitization/description hashing, repair-loop target parsing, targeted repair merge, build-metadata preservation, direct target-file normalization, candidate-ranking aggregate count/rate parsing, weak-trend aggregate count/rate parsing, closed-loop aggregate parsing, submission gate aggregate parsing, baseline/mini-lab aggregate parsing, official/weak execution-control and rate/timeout parsing, official audit/ranking/weak recommendation rate-gate and limit parsing, weak-rerun/trend, strategy-bandit, aggregate registry, and closed-loop touched files, including the deterministic submission packager update
- `git diff --check` reported no whitespace errors, only existing CRLF/LF warnings
- Latest official-eligible candidate table is empty, including when `--min-smoke-contract-axes 1`, `--require-holdout-improvement`, and `--min-holdout-improvement-delta 0.02` are required
- Latest weak-task recommendation dry-run commands include `--min-smoke-contract-axes 1` and `--min-holdout-improvement-delta 0.02`; they do not execute reruns
- `run_weak_task_cleanroom_rerun.py` dry-run for `sharkdp__hexyl.2e26437` prints a closed-loop command with `--skip-official-eval`, `--require-holdout-improvement`, `--min-holdout-improvement-delta 0.02`, and `--min-smoke-contract-axes 1`

## Completion Decision

Do not mark the active goal complete yet. The missing requirement is a verified
local holdout improvement for at least one weak task. The next step is an
authorized local cleanroom rerun, starting with `hexyl`, followed by
`audit_holdout_improvement.py` and `audit_official_eval_gate.py` on the resulting
`result.json`.
