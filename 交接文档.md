# ReBuilder 项目交接文档

## 1. 项目简介
ReBuilder 是面向 ProgramBench 的 cleanroom 程序重建智能体架构，目标是通过自动化 agent 流程，在无源码、无二进制分析、无隐藏测试反馈的条件下，提升多任务重建能力。

## 2. 当前进展
- zoxide 任务官方 aggregate 分数已由 18 提升至 37。
- agourlay__zip-password-finder 任务首次实现官方 aggregate 分数 26，突破非 zoxide 任务；archive/clap strategy pack 闭环后提升到 score 36（248/680 counted；raw eval 340/792）。
- abishekvashok__cmatrix 任务官方 aggregate 分数已由 77 提升至 95（481/508 counted；raw eval 739/769），成为第二个非 zoxide 突破后的新高基线；本地 post-patch replay 为 exploration 64/64、holdout 16/16，但仍不是 fully_resolved/solved task。
- jarun__nnn 任务通过闭环候选实现官方 aggregate 分数 79（379/477 counted；raw eval 1101/1796），成为第三个非 zoxide 官方突破。
- psampaz__go-mod-outdated 任务已完成官方 aggregate baseline：score 15（43/285 counted；raw eval 91/342）。该任务本地 holdout 11/11 但官方分数低，后续应作为泛化差距案例复盘。
- wfxr__csview 任务已完成官方 aggregate baseline：score 57（190/335 counted；raw eval 200/348）。stdin 执行语义修复后本地 min-50 holdout 复核为 8/12，旧 11/12 gate 视为 stale。
- mgdm__htmlq 任务已完成官方 aggregate baseline：score 8（118/1455 counted；raw eval 720/2058）。stdin 执行语义修复后本地 min-50 holdout 复核为 7/11，旧 10/11 gate 视为 stale local-vs-official gap。
- burntsushi__xsv 任务已完成官方 aggregate baseline：score 41（484/1186 counted；raw eval 579/1317）；CSV/xsv strategy pack refinement 后提升到 score 44（518/1186 counted；raw eval 593/1317），本地 holdout 为 9/11。
- tomnomnom__gron 任务已完成官方 aggregate baseline：score 26（117/457 counted；raw eval 117/457）。该结果来自 stdin 执行语义修复与 repair-next 续修后的候选，本地 min-50 holdout 为 11/11，但 exploration 仍为 42/51。
- clog-tool__clog-cli 任务已完成官方 aggregate baseline：score 41（236/575 counted；raw eval 370/778）。该结果来自 task-profile min-50 闭环候选，本地 holdout 为 11/12。
- chmln__sd 任务已完成官方 aggregate baseline：score 86（最新 699/810 counted；raw eval 752/869）。该结果来自 no-external-LLM file_bridge `baseline_regex_patch1` 复核，本地 holdout 为 12/12，runtime smoke 覆盖 args/stdin/input_files/default；这是同分 counted refinement，仍不是 fully resolved。
- rbakbashev__elfcat 任务当前 recorded official aggregate baseline 为 score 56（316/564 counted；raw eval 390/646）。2026-05-21 no-external-LLM file_bridge `reference_html_patch3` 本地闭环为 exploration 119/119、holdout 17/18，并修复 ELF header 记录重复解析的性能风险；官方 eval 约 47:51 后以 ProgramBench `ERROR: RuntimeError` 结束，raw/count 均为 0/0 且无 `.eval.json`，所以只能作为官方评测 operational failure 记录，不是官方突破。closed-loop runner 已补修：命令返回但缺 aggregate JSON 时会清理匹配 ProgramBench 容器/镜像、写 `official_eval_failure_report.json` 并抛错，避免误写 0/0 baseline。
- 2026-05-21 继续修复官方失败证据归因：ranker/planner 不再按 task id 把共享 official-eval root 中的旧失败报告挂到新候选；只有报告位于当前 submission root 下或 metadata 指回该 root 时才接受。embedded `official_eval_summary` 中的 `invalid_eval_payload` / `0/0` 现在归类为 `official_eval_failed_without_eval_json`，不会被当作真实官方 0 分。当前 strict official-ready ranker 仍为空，`elfcat patch3` 正确保留为 official-eval operational failure。
- alecthomas__chroma 任务官方 aggregate 分数已由 3 提升到 13（65/515 counted；raw eval 73/531），来自无外部 LLM 的 file_bridge syntax-highlighter restore/generalization patch（`restore_patch3_generalization_probe`）；本地 exploration/holdout 在 40-case min-holdout gate 下均为 100%，patch3 源码可从 `output/file_bridge_manual/run_chroma_file_bridge.py` 完整复现，仍不是 fully resolved。
- burntsushi__xsv 任务官方 aggregate 分数已由 50 提升到 70（counted 832/1186；raw eval 953/1317；error_code None），来自无外部 LLM 的框架内子代理 file_bridge `restore_patch8`：黑盒差分驱动的多子命令通用算法重写（stats/flatten/fmt/split/cat/headers 等），修复了 patch7 的 `stats -s 列` 崩溃与 `split --size 0` 无限循环；本地差分 86→143/151、0 回归。这是本轮最大的官方突破（+20 score）。注：xsv 官方 eval 操作性较重（需清孤儿容器、设 `PYTHONUTF8=1` 避免 GBK emoji 崩溃、branch-workers=12 约需 30-60min）。
- clog-tool__clog-cli 任务官方 aggregate 分数已由 45 提升到 75（counted 432/575；raw eval 567/778；error_code None），来自无外部 LLM 的框架内子代理 file_bridge `from_latest_patch7`。关键:clog cleanroom 参考是配置依赖退化行为(无 `.clog.toml`→`fatal I/O error with output file` exit 1;有则生成),patch7 据此对齐并修复 help/missing-file/semver;hang 猎杀 0 挂起。这是本轮最大突破(+30)。clog runner 原未跟踪,已 force-add。
- agourlay__zip-password-finder 任务官方 aggregate 分数已由 40 提升到 98（counted 667/680；raw eval 778/792；error_code None），来自无外部 LLM 的框架内子代理 file_bridge PATCH_SOURCE v3。本会话最大突破(+58),接近满分。关键:纯 stdlib WinZip AES PBKDF2 校验(eval 容器无 pyzipper)+ 输出/退出/错误契约对齐;hang 猎杀 0 挂起。注意:AES 2 字节 verifier 在大空间有碰撞假阳性、Python 暴力大空间会慢,官方分布未显现。
- ajeetdsouza__zoxide 任务官方 aggregate 分数已由 37 提升到 67（counted 357/531；raw eval 396/577），来自无外部 LLM 的框架内子代理重建。zoxide 原无 file_bridge runner、init 为 1 行 stub;现 9 个 shell 的 default init 字节级复现(允许的 static asset)+ flag 变体通用替换 + db 通用算法(add 校验/+4.0 frecency/query 排序/--list/--score)。hang 猎杀 0 挂起。重建源码在 `output/file_bridge_manual/zoxide_reconstruct/`。本会话第 5 个突破。
- 相关突破已同步记录于 baselines\programbench\*.baseline.json 及 README.md。
- Windows 本地工程基线已统一到项目内 `.venv`（Python 3.12）；本次整理只运行记录/审计相关 focused tests，未重跑全量 pytest。

## 3. 技术要点
- 只允许 aggregate 结果，禁止用失败细节修复。
- runner/mini-lab 支持 workspace 幂等性与 Docker 镜像自动重建。
- LLM API 评测流程已加重试，优先推进本地信号强的任务。
- file I/O probing 已补到 output directory 场景；结果元数据会记录 phase-level LLM usage（若 provider 返回 usage）。
- 官方闭环新增 min-50 probe、本地 holdout case 数 gate、低样本候选降权排序，避免低样本假阳性直接进入官方评测。
- cleanroom 执行层已修复 stdin 语义：Docker reference 需要 `docker run -i`，replacement 后端对空 stdin 也要传入 `b""` 以关闭输入流。后续排序可用 `scripts\rank_programbench_candidates.py --latest-per-task` 避免旧污染 run 误导。
- repair 闭环已支持回归修复后的最佳代码恢复与失败簇排除重试；多簇失败任务后续可用该逻辑继续榨取 repair 预算。
- probe 泛化已升级为 behavior coverage 驱动：解析 help 中的 flags/subcommands/stdin/file/output 模式，自动补未覆盖 flags、子命令、stdin/file/invalid/error mode，并在结果 metadata 中记录 coverage score 与 missing modes。
- task profile strategy 已接入 spec/implementation/repair 提示链路：从 cleanroom docs、CLI surface 和 probe 输出推断 `network_ping`、`csv_table`、`json_transform`、`html_selector` 等 domain hints，用于提升跨任务实现和 repair 的默认策略。
- task profile strategy 已升级为结构化 strategy pack：为 network/ping、CSV/table、JSON transform、HTML selector、archive/compression、Go dependency report 等领域提供 implementation playbook、repair playbook 和 anti-patterns。
- Docker reference timeout cleanup 已补强：`docker run` 使用唯一 container name，超时后强制清理容器，防止 network/ping 类任务拖死闭环。
- LLM 生成 probe 的 count-like 参数已做运行安全裁剪，避免 `--count=2147483647` 这类长跑探针在本地闭环中反复吃满 timeout。
- ping-like 任务的裸 host 探针会自动加单包 count guard，避免 `pingu 127.0.0.1` 这类默认无限运行行为。
- sheepla__pingu.926d475 已完成结构化 network/ping strategy pack 后的本地闭环复测：holdout 从 3/11（27.3%）提升到 7/12（58.3%），仍未过 gate，官方 eval 未运行。当前结论是 network/ping 实现策略有改善但仍不足，而不是 probe coverage 不足。
- 本轮弱项闭环复跑：gron 最高本地 holdout 9/12（75%）、htmlq/go-mod-outdated 未过 gate、zip-password-finder v2 过 gate 并完成官方 eval，score 从 26 提升到 36；未使用官方隐藏失败细节。
- 继续突破记录：xsv strategy pack v2 过本地 gate 并完成官方 eval，score 从 41 提升到 44；只使用本地 cleanroom 失败簇与官方 aggregate。
- 最新弱项闭环已完成并提交通用 strategy guidance，但均未过本地 gate，未运行官方 eval：
  - `306e765 feat: strengthen gron strategy guidance`：gron exploration 最高到 86.5%，但 holdout 最高仍未过 80% gate（v7 为 8/13=61.5%）；已固化 --stream、--ungron、--no-sort、Go-style JSON diagnostics、numeric lexeme 等通用提示。
  - `79fb145 feat: strengthen htmlq strategy guidance`：htmlq v4 holdout 7/10=70%，v5 劣化后已回退过具体提示；已固化 no-selector skeleton、--filename/--output、pretty layout、Rust panic、remove-nodes 等通用提示。
  - `428d2a1 feat: strengthen go mod strategy guidance`：go-mod-outdated v3 exploration 80.4%、holdout 8/11=72.7%，v4 劣化后已回退；已固化 markdown 固定列宽、odd/even header padding、Go encoding/json literal diagnostics 等通用提示。
  - `ee5ec98 feat: strengthen pingu strategy guidance`：pingu v2 holdout 4/10=40%，低于历史最好 7/12=58.3%；已回退过具体 DNS resolver 提示，仅保留 loopback transcript、special-host diagnostics 等泛化提示。
- 当前仓库最新提交序列为：`ee5ec98`（pingu）、`428d2a1`（go-mod）、`79fb145`（htmlq）、`306e765`（gron）、`1695c1e`（xsv 突破）、`7f2af7a`（zip-password-finder 突破）。

## 4. 目录与关键文件
- baselines\programbench\*.baseline.json：各任务官方分数基线。
- README.md：项目进展与突破说明。
- scripts\run_programbench_mini_lab.py：批量实验入口。
- runs\programbench_official_eval\*：当前官方评测产物；早期实验产物仍可能留在 runs\official_breakthrough_loop\*。

## 5. 后续建议
- 不建议继续盲冲 gron/htmlq/go-mod/pingu 的同类 profile 提示；这些任务近期多轮已出现 holdout 不稳或劣化，继续加提示有过拟合风险。后续只有在新增更强本地 failure cluster 或改进 probe/repair 机制后再回到它们。
- 下一阶段优先做“机制级”优化，而不是继续单任务提示堆叠：
  1. **提升 repair 选择策略**：当前多次出现 repair candidate 降低 exploration/holdout 后被 reject，建议记录每轮 repair diff、失败簇迁移和 reject 原因，做更稳定的 candidate ranking。
  2. **增强 holdout 代表性**：gron/htmlq/go-mod 都出现 exploration 高、holdout 低，建议让 split 覆盖 flag/mode/error/file-output 维度，避免同簇样本集中进 exploration。
  3. **建立 per-domain smoke contracts**：把已沉淀的 gron/htmlq/go-mod/pingu 通用行为抽成小型本地 contract library，让生成/repair 前先过基础行为，而不是只靠 LLM prompt 记忆。
  4. **复盘 local-vs-official gap**：优先分析 go-mod-outdated（本地曾 11/11、官方 score 15）和 htmlq（本地曾过 gate、官方 score 8），找出本地 probe 分布与官方 aggregate 的结构性偏差。
  5. **重新排名候选任务**：使用 `scripts\rank_programbench_candidates.py --latest-per-task` 排除 stale runs，优先选择“本地 gate 稳定 + 官方未跑/低分 + failure 簇少”的任务，而不是继续在低 gate 任务上消耗预算。
- 如果继续单任务突破，建议顺序是：先做 candidate ranking，再选新任务；若必须从旧任务中选，go-mod-outdated 优先于 gron/htmlq/pingu，因为 v3 已接近 gate（8/11=72.7%）且失败簇较少。
- 每次真正过 gate 并跑官方后，继续只记录官方 aggregate/counts，不读取 hidden failure details；若官方 score 突破，更新 `baselines\programbench\*.baseline.json`、README.md、`项目进展与计划.md` 和本交接文档，并提交。

## 6. 联系方式
如需进一步协作或问题反馈，请联系当前维护人。

---
文档更新时间：2026-05-26

