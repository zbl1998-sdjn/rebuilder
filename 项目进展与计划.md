# ReBuilder 项目进展与后续计划

## 一、当前项目情况
- 已实现 zoxide 官方 aggregate 分数 37（前为 18），突破原有基线。
- agourlay__zip-password-finder 任务首次实现官方 aggregate 分数 26，成为首个非 zoxide 突破；archive/clap strategy pack 闭环后进一步提升到 score 36（248/680 counted；raw eval 340/792）。
- abishekvashok__cmatrix 任务官方 aggregate 分数已由 77 提升至 95（481/508 counted；raw eval 739/769），来自 historical adaptive_profile 的 cleanroom-local patch；本地 post-patch replay 为 exploration 64/64、holdout 16/16，但仍不是 fully_resolved/solved task。
- jarun__nnn 任务通过闭环候选实现官方 aggregate 分数 79（379/477 counted；raw eval 1101/1796），成为第三个非 zoxide 官方突破。
- psampaz__go-mod-outdated 任务通过 min-50 holdout 闭环完成官方 aggregate baseline：score 15（43/285 counted；raw eval 91/342）。本地 holdout 为 11/11，但官方分数较低，作为 local-vs-official gap 样本保留。
- wfxr__csview 任务通过 min-50 holdout 闭环完成官方 aggregate baseline：score 57（190/335 counted；raw eval 200/348）。修复 stdin 转发后重新本地验证为 8/12 holdout，旧 11/12 本地 gate 视为 stale。
- mgdm__htmlq 任务官方 aggregate 分数已由 8 提升至 91（1330/1455 counted；raw eval 1881/2058），来自 no-external-LLM file_bridge restoration patch；本地 exploration 为 48/49、holdout 为 14/15，runtime smoke 覆盖 args/stdin/input_files/default。该结果是 aggregate 官方基线升级，不是 solved task。
- burntsushi__xsv 任务在 stdin 执行修复和 repair-next 续修后完成官方 aggregate baseline：score 41（484/1186 counted；raw eval 579/1317）；CSV/xsv strategy pack refinement 后进一步提升到 score 44（518/1186 counted；raw eval 593/1317），本地 holdout 为 9/11。
- tomnomnom__gron 任务官方 aggregate 分数已由 26 提升至 62（140/224 counted；raw eval 148/233），来自 no-external-LLM file_bridge restoration patch；本地 exploration 为 45/45、holdout 为 13/14，runtime smoke 覆盖 args/stdin/input_files/default。该结果是 aggregate 官方基线升级，不是 solved task。
- clog-tool__clog-cli 任务官方 aggregate 分数已由 41 提升至 45（257/575 counted；raw eval 391/778），来自 no-external-LLM file_bridge restoration patch；本地 exploration 为 99/106、holdout 为 13/16，runtime smoke 覆盖 args/stdin/input_files/default。该结果是 aggregate 官方基线升级，不是 solved task。
- chmln__sd 任务最新官方 aggregate baseline 为 score 86（699/810 counted；raw eval 752/869），来自 no-external-LLM file_bridge `baseline_regex_patch1` 复核；本地 holdout 为 12/12，runtime smoke 覆盖 args/stdin/input_files/default。这是同分 counted refinement，仍不是 fully resolved。
- rbakbashev__elfcat 任务当前 recorded official aggregate baseline 为 score 56（316/564 counted；raw eval 390/646）。2026-05-21 的 no-external-LLM file_bridge `reference_html_patch3` 本地闭环达到 exploration 119/119、holdout 17/18，并修复了 ELF header 记录重复解析的性能风险；但官方 eval 约 47:51 后以 ProgramBench `ERROR: RuntimeError` 结束，raw/count 均为 0/0，且没有写出 `.eval.json`，因此不更新 baseline，也不能称为官方突破。随后已修复 closed-loop runner 对这类缺失 `.eval.json` 的处理：命令返回但无 aggregate JSON 时会清理匹配 ProgramBench 容器/镜像、写 `official_eval_failure_report.json` 并抛错，避免误记录为 0/0 baseline。
- 2026-05-21 继续修复官方失败证据归因：`rank_programbench_candidates.py` 现在只接受属于当前候选 submission root 的 `official_eval_failure_report.json`，不再按 task id 挂接旧报告；embedded `official_eval_summary` 里的 `invalid_eval_payload` / `0/0` 也会被归为 `official_eval_failed_without_eval_json`。复核后 strict official-ready ranker 仍为 `row_count=0`，`elfcat patch3` 正确显示为 official-eval operational failure 且不再误指向 patch2 报告。
- 已从已有官方 eval aggregate 产物补记两个历史低样本 baseline：rbakbashev__elfcat score 17（96/564 counted，本地 holdout 仅 2 cases）和 alecthomas__chroma score 3（13/515 counted，本地 holdout 仅 5 cases）；它们只作为历史 aggregate 证据，不作为当前 gate-passing 候选。
- 相关分数已记录于 baselines\programbench\*.baseline.json，并同步至 README.md。
- Windows 本地开发基线已统一到项目内 `.venv`（Python 3.12）；本次整理只运行记录/审计相关 focused tests，未重跑全量 pytest。
- runner/mini-lab 支持 workspace 幂等性，Docker 镜像拉取失败可自动重建。
- LLM API 评测流程已加重试，优先推进本地信号强的任务。
- 当前闭环推进已完成并固化 cmatrix、jarun__nnn、go-mod-outdated、csview、htmlq、xsv、gron、clog-cli 与 chmln__sd 新基线；剩余候选任务持续推进中。
- cleanroom 执行层已修复 stdin 语义：Docker reference 增加 `-i`，local/WSL/Docker replacement 对空 stdin 也显式关闭输入流。后续候选排序应优先使用 `rank_programbench_candidates.py --latest-per-task`，避免旧污染 run 误导。
- 候选排序已支持 `--official-eligible-only`，只显示未做过官方 eval 且通过 aggregate 本地 holdout rate/case gate 的候选，并输出 `official gate` 阻断原因；当前该过滤结果为空，pingu 的主要阻断原因是 `low_holdout_rate`，因此下一步应继续本地 cleanroom 泛化，而不是提交官方 eval。
- 新增 `scripts\audit_official_eval_gate.py`，可对单个 `result.json` 输出 aggregate-only official gate JSON，并在 `low_holdout_rate`、`too_few_holdout_cases` 等阻断原因下返回非零退出码，作为手动 official eval 前的硬检查。
- 新增 `scripts\summarize_holdout_trends.py`，用于按任务汇总最新 reliable holdout 与历史 best reliable holdout 的 aggregate-only 趋势；默认要求至少 10 个 holdout case，避免低样本 run 误判为进展。
- `scripts\summarize_holdout_trends.py --recommend-weak-reruns --include-rerun-command` 可基于 aggregate-only 趋势输出下一批本地弱项 rerun 目标，并强制显示 `--skip-official-eval --require-holdout-improvement --holdout-history-root runs` 等准入护栏和 guarded wrapper dry-run 命令；当前真实 runs 下推荐 hexyl、zoxide、pingu。
- 新增 `scripts\run_weak_task_cleanroom_rerun.py` 作为弱项本地 rerun 安全入口：默认只 dry-run，只有显式 `--execute` 才执行 local cleanroom closed-loop；执行命令固定带 `--skip-official-eval` 与 `--require-holdout-improvement`，且不暴露 official eval run-name/root 参数；已用 hexyl dry-run 验证命令构造。
- 新增 `scripts\audit_holdout_improvement.py`，可对单个 rerun 做 aggregate-only 弱项改善审计；只有当前 holdout 超过该任务历史 reliable best 才返回 0，pingu v3 当前被判定为 `not_improved`。
- closed-loop runner 的 aggregate-only strategy registry 接入已补主流程测试：策略 variant 会在 ReBuilder 执行前应用，`--skip-official-eval` 和 holdout gate 阻断不会追加 registry 反馈行。
- closed-loop runner 新增可选 `--require-holdout-improvement --holdout-history-root runs`，用于弱项 rerun：即使本地 holdout 过 0.8 gate，只要没有超过历史 reliable best，也会在 package/official eval 前以 gate skip 停止。
- strategy ablation runner 已透传 `--require-holdout-improvement --holdout-history-root runs`，用于弱项 ablation：每个 variant 先通过 aggregate local holdout 改善 gate，才可能进入 package/official-eval 路径；gate skip 仍按本地阻断处理，不写入官方反馈。
- repair 闭环已支持在单次 repair 回归后恢复最佳代码，并排除该失败簇继续尝试下一个簇，避免 xsv 这类多簇失败样本因为一次坏修复直接停摆。
- probe 泛化已从“凑 min_samples”升级为 behavior coverage 驱动：会解析 help 中的 flags/subcommands/stdin/file/output 模式，针对未覆盖 flags、子命令、stdin/file/invalid/error mode 自动补 probe，并把 coverage score 写入结果 metadata。
- spec/implementation/repair 链路已加入 task-domain profile：从 docs、CLI surface 和 probes 识别 `network_ping`、`csv_table`、`json_transform`、`html_selector` 等策略域，并把 domain-specific hints 注入提示词，但仍以 exact behavior contracts 为最高优先级。
- task-domain profile 已升级为结构化 strategy pack：每个领域包含 implementation playbook、repair playbook 和 anti-patterns，当前覆盖 network/ping、CSV/table、JSON transform、HTML selector、archive/compression、terminal UI、Go dependency report、filesystem tool、binary/hexdump tool。
- adaptive deterministic probes 已覆盖 binary/hexdump 轴：空 stdin、raw binary 文件、length/skip offset、panel layout、invalid color-scheme 诊断，用于 hexyl-style 弱项本地 cleanroom 泛化。
- Docker reference 执行层已加强 timeout cleanup：每次 `docker run` 带唯一 container name，超时后强制 `docker rm -f`，避免 ping/network 类探针无限挂住闭环。
- LLM 生成的 count-like 长跑探针已加安全裁剪：`-c`、`--count`、`--repeat`、`--iterations` 等参数会被限制到正向小样本值，避免把行为探针变成 soak test。
- ping-like 任务的裸 host 探针会自动加单包 count guard，避免默认 ping 行为无限运行。
- sheepla__pingu.926d475 已用结构化 network/ping strategy pack 重跑闭环，本地 holdout 从 3/11（27.3%）提升到 7/12（58.3%），但仍未达 gate，按规则跳过官方 eval；该任务继续作为 network/ping 泛化差距样本复盘。
- 授权后的 pingu v3 本地 cleanroom rerun 已带 `--skip-official-eval` 执行，未调用官方评测；本地 aggregate 为 exploration 28/61（45.9%）、holdout 3/14（21.4%），官方准入阻断原因仍是 `low_holdout_rate`。holdout trend 审计显示它相对可靠 best 7/12（58.3%）进一步回退，因此下一次本地 cleanroom rerun 必须先超过 58.3% 才能算弱项 holdout 改善。已将公开 exploration 观察到的 special address taxonomy、Go/net resolver 文案、zero-transmitted 网络错误 transcript、完整 packet statistics、pingu dot-art packet-line prefix 和 `-c, --count` Go 风格 parse-error alias 规则补入 network/ping strategy pack，并把 address-category state machine 前移到 implementation playbook；下一步不应进入官方 eval。
- 弱项闭环复跑结论：gron 最高到本地 holdout 9/12（75%），htmlq/go-mod-outdated 仍未过 gate；zip-password-finder v2 过 gate 并完成官方 eval，score 从 26 提升到 36。全程未使用官方隐藏失败细节。
- 本轮继续突破：xsv strategy pack v2 过本地 gate（9/11）并完成官方 eval，score 从 41 提升到 44；全程仍只使用本地 cleanroom 失败簇与官方 aggregate。
- cmatrix 官方 strategy ablation 已完成：baseline_no_adaptive 得到 score 88（445/508 counted；raw eval 705/769），adaptive_profile 得到 score 93（471/508 counted；raw eval 729/769）并升级 baseline；adaptive_deep 因本地 holdout 16/21（76.2%）低于 80% gate，按规则未触发官方 eval。2026-05-18 在不使用外部 LLM 的官方验证边界下，对历史 adaptive_profile 候选做 cleanroom-local patch，post-patch replay 为 exploration 64/64、holdout 16/16，官方 aggregate 进一步提升到 score 95（481/508 counted；raw eval 739/769）；`fully_resolved=false`，所以这是 score 档突破，不是 solved task。
- 2026-05-15 继续围绕“架构优化优先于参数堆叠”做 cleanroom 突破尝试，但未产生新的官方 score 更新，因此未改 baseline/README。已沉淀的通用架构改动包括：ProgramBench 文档发现支持 `.mkd`/多文档合并，`find_replace`、`json_transform`、`html_selector`、`csv_table`、`archive_compression` 自适应行为轴补强，以及 differential suite 对 LLM 生成 unsafe `input_files` 的过滤，避免无效绝对路径探针打断整轮验证。验证为 `619 passed`。
- 2026-05-15 本地 gate 证据：`gron` 因 `.mkd` 修复从 0 字符文档提升到 8858 字符文档，但 holdout 为 9/16（56.2%）；`htmlq` 行为轴补强后一度 exploration 达到 61/63（96.8%），但 holdout 10/15（66.7%）；`xsv` CSV 子命令补强轮因生成代码语法错误 holdout 0/16；`zip-password-finder` password/archive flag 补强轮 holdout 3/12（25.0%）。这些 run 均未过 80% 本地 gate，因此均未触发官方评测。
- 2026-05-15 已完成 implementation 阶段 Python runtime smoke gate 第一版：生成后会先做静态 integrity 检查，再实际运行入口的 `<no args>`、`--help` 和最多 6 个安全 behavior-contract CLI 形态；若出现 Python traceback、执行异常或 timeout，会把 `runtime_smoke_*` issue 回传给 retry prompt。staged support module 若引入运行时崩溃，会被拒绝并回退到 entrypoint 版本。
- 2026-05-15 已完成 task profile prompt 动态预算控制第一版：正常内置 profile 保持完整；当 profile payload 过大时，自动裁剪 domains、hints、strategy playbook、anti-patterns 和 evidence keywords，并用 `__truncated__` 标记，避免新增 probe/profile 后 implementation prompt 继续膨胀。聚焦 ruff 通过，`.venv` 下全量验证为 `626 passed`。
- 2026-05-15 针对“已有官方 baseline 的小幅升级”补齐 aggregate-only 审计路径：`rank_programbench_candidates.py` 与 `audit_official_eval_gate.py` 新增 `--allow-existing-official`，默认仍阻断 `already_official`，只有显式开启时才把通过本地 gate 的既有任务标为 `eligible_baseline_upgrade`。真实 runs 下普通官方候选表仍为空；开启 baseline upgrade 后可看到当前 `clog-cli`、`nnn` baseline 级候选，但叠加 `--require-holdout-improvement --min-holdout-improvement-delta 0.02` 后仍为空。
- 2026-05-15 推荐的 `hexyl` local-only rerun dry-run 已确认会带 `--skip-official-eval --require-holdout-improvement`，不会触发官方隐藏评测；实际 `--execute` 被安全审查阻止，因为流程会向外部 LLM 服务发送本地提示/代码上下文并写 run 产物。本轮未绕过该限制，未新增官方分数。
- 2026-05-15 新增 `scripts\audit_official_baseline_candidates.py`，只读比较已有官方 `*.eval.json` aggregate 结果和 recorded baselines；发现并补记两个历史低样本官方非零 baseline：`rbakbashev__elfcat.52f8cc7` score 17（96/564 counted，本地 holdout 只有 2 cases）和 `alecthomas__chroma.8d04def` score 3（13/515 counted，本地 holdout 只有 5 cases）。补记后 actionable 审计表为空；这些是历史 aggregate 证据，不作为当前 80% gate 下的新提交候选。
- 2026-05-15 新增 `scripts\plan_official_breakthrough_targets.py`，把 recorded official baseline aggregate 分数和本地 latest/best reliable holdout trend 合成“官方突破目标队列”。当前真实 runs 下：`clog-cli`、`nnn` 是 `ready_baseline_gate`；`htmlq`、`go-mod-outdated`、`gron`、`zip-password-finder`、`xsv`、`csview`、`cmatrix` 是 `restore_historical_gate`；`zoxide` 是 `weak_cleanroom_rerun`；`chroma`、`elfcat` 缺当前可靠 holdout。该队列只读 aggregate 字段，不读取 hidden/detail failure。
- 2026-05-15 `plan_official_breakthrough_targets.py --include-next-command` 已补齐 restore 类目标的下一步命令：对历史 best `result.json` 输出 `audit_official_eval_gate.py ... --allow-existing-official`。已实际审计 `htmlq`、`gron`、`xsv`、`zip-password-finder` 的历史 best，本地 aggregate gate 均为 `eligible_baseline_upgrade`；这证明这些任务当前主要是最新机制回退问题，应先做恢复/ablation，不应提交最新退化 run。
- 2026-05-15 `plan_official_breakthrough_targets.py` 新增 `--include-restore-ablation-command`，可为 restore 行直接输出 guarded strategy ablation dry-run。真实 runs 下已生成 `htmlq`、`go-mod-outdated`、`gron`、`zip-password-finder`、`xsv`、`csview`、`cmatrix` 的 dry-run 命令，均带 `--skip-official-eval`、`--require-holdout-improvement`、`--holdout-history-root runs`、`--max-generalization-risk low` 和 smoke-axis gate。
- 2026-05-15 新增 `scripts\run_restore_axis_ablation_batch.py`，作为 restore-axis 批量入口：只选择 aggregate-only planner 的 `restore_historical_gate` 行，默认只打印 guarded strategy ablation 命令；只有显式 `--execute` 才调用子进程。真实 runs 下 dry-run smoke 已输出 7 个 restore 目标命令，未执行外部 LLM、Docker 或 official eval。
- 2026-05-15 新增 `scripts\audit_restore_targets.py`，专门审计 `restore_historical_gate` 任务。真实 runs 下：`htmlq` latest 43.8% vs best 90.9%（回退 -47.2%）、`gron` latest 56.2% vs best 100.0%（回退 -43.8%）、`zip-password-finder` latest 25.0% vs best 100.0%（回退 -75.0%）、`xsv` latest 0.0% vs best 81.8%（回退 -81.8%）。其中 `htmlq/gron/zip/xsv` latest axes 均由历史 best 的 `0/0` 增加到非零，被标记为 `new_axis_expansion_regression`，下一步应做新增 probe/strategy axis 的 ablation。
- 2026-05-15 新增 `scripts\audit_generalization_risk.py` 作为 official eval 前的 aggregate-only 反过拟合 gate。真实 runs 用 `--fail-on-risk high` 会阻断缺可靠 holdout 的 `chroma/elfcat`、低于 gate 的 `zoxide`、所有 latest 退化 restore 任务，以及 `htmlq/gron/zip-password-finder/xsv` 的 `new_axis_expansion_regression`。`clog-cli/nnn` 当前为 low risk，但只代表可进入 baseline-upgrade/holdout-improvement 审计，不代表直接提交官方评测。
- 2026-05-15 `package_submission.py`、`run_official_closed_loop.py` 与 `run_official_strategy_ablation.py` 已接入 `--max-generalization-risk low|medium|high`，闭环 runner 和 strategy ablation 会把 aggregate-only 泛化风险门禁传给打包器；高风险候选会在 archive/official eval 前停止。已用 htmlq 高风险候选 smoke 验证会因 `new_axis_expansion_regression` 被阻断，避免最新退化 run 误入官方评测。
- 2026-05-15 已把 11 个 task-domain strategy skills 全面升级为带 `generalization_playbook` 的结构化 pack：network_ping、csv_table、json_transform、html_selector、archive_compression、terminal_ui、go_dependency_report、filesystem_tool、terminal_animation、binary_hexdump、find_replace 均新增 unseen holdout、CLI dispatch、stdin/file、error branch、bounded smoke 等泛化检查。`TaskProfile` 类型、profile rule loader、implementation/repair prompt 都已接入该字段，避免只把 skills 当作实现提示而缺少反过拟合验证。
- 2026-05-15 adaptive deterministic probes 已覆盖全部 11 个 strategy domains。新增 `terminal_ui`、`filesystem_tool`、`terminal_animation` 的本地 smoke axes 后，真实代码统计为 network 9、csv 11、json 10、html 17、archive 8、terminal_ui 6、go_dependency 6、filesystem 7、terminal_animation 7、binary 6、find_replace 9；这样所有 strategy skills 都同时具备 prompt guidance 和本地泛化验证入口。
- 2026-05-15 新增 `scripts\audit_strategy_domain_coverage.py --fail-on-missing`，用于本地审计 11 个 strategy skills 是否同时具备 `generalization_playbook`、adaptive probes、`smoke_contract` axes 和 `adaptive_axis` axes。当前全部 domain 状态为 `ok`，该 gate 不读取 holdout/official hidden failure。
- 2026-05-15 repair 阶段已接入 Python integrity/runtime smoke gate：每次 repair 候选生成后，若有语法错误、缺 entrypoint、entrypoint 不 dispatch、缺 import 或 runtime smoke traceback，会记录为 `repair_integrity_failed` 并恢复上一版 accepted codebase，避免 xsv 这类 0% syntax-broken 修复被 accepted。
- 2026-05-15 11 个 task-domain strategy skills 已全面补齐 `validation_playbook`：每个 domain 都有 3 条 cleanroom-local 验证清单，覆盖 smoke、holdout 维度拆分、parser/IO/error 分支和非回归检查；`TaskProfile`、implementation/repair prompt、`audit_strategy_domain_coverage.py --fail-on-missing` 均已接入。该改造只使用通用领域验证哲学，不包含官方 hidden failure 或任务特判答案。
- 2026-05-15 `audit_strategy_domain_coverage.py --fail-on-missing` 已新增 strategy skill cleanroom-policy lint：若 domain pack 出现 `official`、hidden-test 细节、test-set/leaderboard/eval score 语汇或具体 ProgramBench task id，会把该 domain 标为 `missing_cleanroom_policy` 并失败。当前 11 个 domain 的 cleanroom issues 均为 0，确保 skills 仍是领域通用归纳偏置，而不是测试集答案表。
- 2026-05-16 已刷新 active goal completion audit，并写入 `docs\programbench-cleanroom-goal-audit.md`：严格 official-eligible 表在 `--min-smoke-contract-axes 1 --require-holdout-improvement --min-holdout-improvement-delta 0.02` 下仍为空；`audit_generalization_risk.py --fail-on-risk high` 仍因 missing reliable holdout、latest holdout regression、new-axis expansion regression 返回阻断；strategy skill coverage/cleanroom gate 全绿；全量 `.venv` pytest 为 `664 passed`。因此当前目标仍未完成，下一步必须是显式授权后的 guarded restore-axis ablation 或 weak-task rerun。
- 2026-05-15 `run_official_strategy_ablation.py` 新增 `--dry-run`，用于先生成 restore-axis ablation 的 child closed-loop 命令而不执行外部 LLM、Docker、packaging 或 official eval。已对 `htmlq`、`gron`、`xsv`、`zip-password-finder` 生成 dry-run 命令，均保留 `--skip-official-eval`、`--require-holdout-improvement`、`--max-generalization-risk low` 与 smoke-axis gate；本轮又用 `htmlq` 的 `baseline_no_adaptive/adaptive_profile` 做了 fresh dry-run smoke，exit 0 且只打印 child 命令。实际执行前仍需显式授权。
- 2026-05-16 外部执行确认门已补齐到直接入口：`run_official_closed_loop.py` 没有 `--ack-external-llm-docker` 会在加载样本/运行子命令前退出；非 dry-run 的 `run_official_strategy_ablation.py` 也要求同一确认并传给 child closed-loop。`run_restore_axis_ablation_batch.py` 和 `run_weak_task_cleanroom_rerun.py` 在 `--execute --ack-external-llm-docker` 时会继续把确认传下去；`run_programbench_mini_lab.py` 也要求 ack 后才加载 catalog/准备任务/调用 ReBuilder。聚焦测试 122 passed，`python -m ruff check ...` 通过，本轮新增 planner/restore JSON dry-run command plan 后全量 `.venv` pytest 为 676 passed in 8.98s；没有触发外部 LLM、Docker 或官方评测。
- 2026-05-16 `audit_restore_targets.py` 新增 cleanroom-local axis delta：只从 `implementation_metadata.probe_axis_coverage` 的本地轴名读取 `domain.axis`，输出 `added axes` / `removed axes` / `axis action`，不读取 hidden failure。真实 runs 显示 `htmlq -> ablate_added_axis_domains:html_selector`、`gron -> json_transform`、`zip-password-finder -> archive_compression,json_transform`、`xsv -> csv_table`；`go-mod-outdated/csview/cmatrix` 为 same-axis regression，标记 `inspect_same_axis_strategy_regression`。下一轮可按这些通用轴做 restore/ablation，而不是针对测试集失败补丁。
- 2026-05-16 `audit_restore_targets.py --format json` 已可输出 machine-readable cleanroom payload：包含 `schema_version`、`row_count`、`total_row_count` 以及每行 aggregate gate、axis delta 和 `axis_delta_action`，不展开原始 `result.json` / baseline 详情。真实 `runs` smoke 用 `--limit 2 --format json` 返回 `row_count=2`、`total_row_count=7`，可供后续 batch/dashboard 直接消费，避免解析 markdown。
- 2026-05-16 `run_restore_axis_ablation_batch.py` 已接入 restore audit 的 axis action：新增 `--axis-action-domain <domain>` 和 `--show-axis-action`，可按 cleanroom domain 过滤 restore 目标并打印带 action 标注的 guarded dry-run 命令。真实 `runs` 下 `--axis-action-domain csv_table --show-axis-action --limit 7` 只选中 `burntsushi__xsv.f430466`，命令仍保留 `--skip-official-eval`、holdout improvement、generalization risk 和 smoke-axis gate，未执行外部流程。
- 2026-05-16 `run_restore_axis_ablation_batch.py --format json` 已可输出 machine-readable dry-run command plan，供后续 batch/dashboard 直接消费 command array，不再解析人类日志。JSON 模式只支持 dry-run，非 dry-run execute 会拒绝，避免子进程日志混入 JSON。真实 `runs` smoke 用 `--axis-action-domain csv_table --show-axis-action --limit 7 --format json` 返回 `row_count=1`，唯一目标是 `burntsushi__xsv.f430466`，command 仍包含 `--skip-official-eval` 和 `--dry-run`。
- 2026-05-16 `plan_official_breakthrough_targets.py --format json` 已可输出完整 official breakthrough target queue 的 aggregate-only JSON。真实 `runs` smoke 用 `--include-next-command --include-restore-ablation-command --limit 12 --format json` 返回 `row_count=12`、`total_row_count=12`，覆盖 `ready_baseline_gate`、`restore_historical_gate`、`weak_cleanroom_rerun`、`missing_reliable_holdout` 四类；`next_command` 只含 guarded dry-run 或审计命令，不读取/展开 hidden failure 细节。
- 2026-05-16 新增 `scripts\run_missing_holdout_cleanroom_rerun.py`，专门处理 `chroma`、`elfcat` 这类已有历史 official aggregate baseline 但缺当前可靠本地 holdout 的任务。该 wrapper 默认只 dry-run，child closed-loop 命令固定带 `--skip-official-eval`，不带 `--require-holdout-improvement`，因为此阶段先建立可靠 local signal，尚无 reliable previous holdout 可比较；真实执行仍必须显式 `--execute --ack-external-llm-docker`。
- 2026-05-16 `plan_official_breakthrough_targets.py` 新增 `--include-missing-holdout-command`、`--missing-holdout-rerun-root`、`--missing-holdout-min-smoke-contract-axes`。真实 `runs` smoke 用 `--include-next-command --include-missing-holdout-command --include-restore-ablation-command --limit 12 --format json` 返回 `row_count=12`，并为 `alecthomas__chroma.8d04def`、`rbakbashev__elfcat.52f8cc7` 输出本地 missing-holdout dry-run 调度命令。
- 2026-05-16 `plan_official_breakthrough_targets.py` 进一步新增 ready-baseline strict command flags：`--baseline-upgrade-min-smoke-contract-axes`、`--baseline-upgrade-require-holdout-improvement`、`--baseline-upgrade-min-holdout-improvement-delta`。真实 `runs` smoke 加上这些 flags 后，`clog-cli` 与 `nnn` 的 `next_command` 会把 `rank_programbench_candidates.py` 命令收紧到 smoke-axis + holdout-improvement gate，避免 low-risk ready 行被误读成可直接官方提交。
- 2026-05-16 本轮 focused verification：`tests/test_run_missing_holdout_cleanroom_rerun.py` 与 `tests/test_plan_official_breakthrough_targets.py` 合计 `14 passed`；`python -m ruff check` 通过 planner/wrapper touched files；全量 `.venv` pytest 为 `684 passed in 9.69s`。wrapper/planner dry-run smoke 只打印命令，未执行外部 LLM、Docker 或官方评测。
- 2026-05-16 `audit_generalization_risk.py` 新增 `--format json`，可把 official eval 前的反过拟合 gate 输出为 machine-readable aggregate-only payload。真实 `runs` smoke 返回 `schema_version=1`、`row_count=12`、`total_row_count=12`；加 `--fail-on-risk high --format json` 时仍非零退出但 stdout 保持可解析 JSON，只包含官方 aggregate、本地 holdout aggregate、risk level/reason、路径和 required next action，不展开 hidden failure 细节。
- 2026-05-16 本轮 JSON 风险审计验证：`tests/test_audit_generalization_risk.py` 为 `4 passed`；`python -m ruff check scripts\audit_generalization_risk.py tests\test_audit_generalization_risk.py` 通过；严格 official-eligible 表仍为空，`audit_generalization_risk.py --fail-on-risk high` 仍阻断 high-risk 行，strategy domain coverage 11 个 domain 全部 `ok`，全量 `.venv` pytest 为 `685 passed in 9.34s`。未触发外部 LLM、Docker 或官方评测。
- 2026-05-16 `audit_strategy_domain_coverage.py` 新增 `--format json`，把 11 个 strategy domain 的 generalization/validation/cleanroom/probe 覆盖状态输出为 machine-readable aggregate-only payload。真实 smoke 返回 `schema_version=1`、`row_count=11`、`total_row_count=11`，全部 domain 为 `ok`；JSON 不打印 markdown banner，也不输出 strategy pack 原文或 matched cleanroom-policy 片段，方便后续 batch/dashboard 把 skills cleanroom 哲学作为硬 gate。
- 2026-05-16 本轮 strategy coverage JSON 验证：先用新增 CLI 测试确认缺 `--format json` 时失败，再实现后 `tests/test_audit_strategy_domain_coverage.py` 为 `4 passed`；`python -m ruff check scripts\audit_strategy_domain_coverage.py tests\test_audit_strategy_domain_coverage.py` 通过；全量 `.venv` pytest 为 `686 passed in 10.06s`。
- 2026-05-16 `rank_programbench_candidates.py` 新增 `--format json`，把 candidate ranking / strict official-eligible gate 输出为 aggregate-only JSON。真实 strict gate smoke 用 `--official-eligible-only --latest-per-task --min-smoke-contract-axes 1 --require-holdout-improvement --min-holdout-improvement-delta 0.02 --format json` 返回 `row_count=0`、`total_row_count=0`、`rows=[]`；普通 `--latest-per-task --limit 3 --format json` 只输出 local/holdout aggregate、axis counts、gate reason、status、official marker 和 result path。
- 2026-05-16 本轮 candidate-ranking JSON 验证：先用隐藏 marker 回归测试确认旧 CLI 因缺 `--format json` 失败，再实现后 `tests/test_rank_programbench_candidates.py` 为 `57 passed`；`python -m ruff check scripts\rank_programbench_candidates.py tests\test_rank_programbench_candidates.py` 通过；全量 `.venv` pytest 为 `687 passed in 9.60s`。
- 2026-05-16 `summarize_holdout_trends.py` 新增 `--format json`，把 latest-vs-best holdout trend 和 weak-task rerun recommendations 输出为 aggregate-only JSON。真实 smoke 用 `--recommend-weak-reruns --include-rerun-command --rerun-min-smoke-contract-axes 1 --rerun-min-holdout-improvement-delta 0.02 --format json` 返回 trend `row_count=3`/`total_row_count=13`，recommendation `row_count=3`/`total_row_count=4`；top dry-run 目标仍为 `hexyl`、`zoxide`、`pingu`，命令保留 `--dry-run`、smoke-axis gate 和 holdout-improvement delta gate。
- 2026-05-16 本轮 weak-task trend JSON 验证：先用隐藏 marker 回归测试确认旧 CLI 因缺 `--format json` 失败，再实现后 `tests/test_summarize_holdout_trends.py` 为 `31 passed`；`python -m ruff check scripts\summarize_holdout_trends.py tests\test_summarize_holdout_trends.py` 通过；全量 `.venv` pytest 为 `688 passed in 9.69s`。
- 2026-05-16 新增 `scripts\run_weak_task_cleanroom_rerun_batch.py`，批量消费 aggregate-only weak-task recommendations。默认只 dry-run，`--format json` 只输出命令计划；真实 `runs` smoke 返回 `row_count=3`，目标为 `hexyl`、`zoxide`、`pingu`，每条 child `run_weak_task_cleanroom_rerun.py` 命令都带 `--dry-run`、`--min-smoke-contract-axes 1`、`--min-holdout-improvement-delta 0.02`，未执行外部流程。
- 2026-05-16 本轮 weak-task batch wrapper 验证：新增测试先因模块不存在失败，再实现后 `tests/test_run_weak_task_cleanroom_rerun_batch.py` 为 `7 passed`；`python -m ruff check scripts\run_weak_task_cleanroom_rerun_batch.py tests\test_run_weak_task_cleanroom_rerun_batch.py` 通过；全量 `.venv` pytest 为 `695 passed in 9.75s`。
- 2026-05-16 behavior contract 已保留 cleanroom 观察里的安全相对 `input_files` 与 input previews；implementation prompt 会展示文件名和有界预览，Python runtime smoke 会把这些输入文件写入临时 workdir 后运行安全 contract CLI case，补齐文件模式 entrypoint dispatch 检查，仍不使用官方 hidden failure。
- 2026-05-16 本轮 file-input runtime smoke 验证：新增 TDD 测试先失败于 `BehaviorContract` 缺 `input_files` 字段、prompt 缺 input preview、runtime smoke 跳过文件参数 contract；实现后 focused `3 passed`，相关 spec/prompt/integrity/repair 测试 `75 passed`，`python -m ruff check` 通过 touched files，全量 `.venv` pytest 为 `696 passed in 10.23s`。
- 2026-05-16 spec synthesis 进一步过滤 unsafe input-file prompt 泄漏：`_format_corpus()` 只展示安全相对 input file 的名称和预览，丢弃 unsafe 路径及内容；argv 中引用被丢弃 unsafe input file 时会 redacted 为 `<unsafe_input_file>`，runtime smoke 也会跳过 `../...` 等 unsafe 文件路径 argv。
- 2026-05-16 本轮 unsafe input-file 边界验证：新增测试先确认 unsafe 文件名/内容会进入 spec prompt、unsafe argv 会被 runtime smoke 执行；实现后安全测试 `2 passed`，相关 spec/prompt/integrity 测试 `59 passed`，`python -m ruff check` 通过 touched files，全量 `.venv` pytest 为 `698 passed in 9.93s`。
- 2026-05-16 behavior contract 现在也保留安全 env vars：新增 `core/execution/env.py` 共享过滤器，只允许合法变量名、短值，并过滤 token/secret/password/key/credential/auth 等敏感名称。Spec prompt、implementation behavior contract prompt 和 Python runtime smoke 都只使用过滤后的 env vars，使 `TERM/COLUMNS/LINES/NO_COLOR` 等 terminal cleanroom probe 能参与实现阶段 smoke。
- 2026-05-16 本轮 env-var contract 验证：新增 TDD 测试先失败于 spec prompt 缺 env、behavior contract prompt 缺 env、runtime smoke 未传 `TERM=unknown`；敏感 `API_TOKEN` case 保持不触发。实现后 focused `4 passed`，相关 spec/prompt/integrity/execution-backend 测试 `81 passed`，`python -m ruff check` 通过 touched files，全量 `.venv` pytest 为 `702 passed in 9.98s`。
- 2026-05-16 runtime smoke 去重键已从 `args/stdin` 扩展为 `args/stdin/input file content fingerprints/env vars`，避免 terminal env-only contract 被默认 `<no args>` smoke 吞掉，也避免同 argv 但不同输入文件内容的 contract 被误判重复。
- 2026-05-16 本轮 runtime smoke identity 验证：新增 TDD 测试先确认 `TERM=unknown` 且空 argv/stdin 的 contract 会被旧去重跳过；实现后相关 spec/prompt/integrity 测试 `64 passed`，`python -m ruff check core\codebase\runtime_smoke.py tests\test_codebase_integrity.py` 通过，全量 `.venv` pytest 为 `703 passed in 10.13s`。
- 2026-05-16 spec synthesis 现在会 redacted 原始 argv 中的 unsafe file-like path，即使该路径没有出现在 `input_files` map 中；覆盖绝对路径、Windows drive-qualified path 和 parent traversal path，URL 形态不按文件路径处理。这样无效本地路径不会进入 observation prompt 或 exact behavior contract args。
- 2026-05-16 本轮 raw argv redaction 验证：新增 TDD 测试先确认 `C:\Users\Administrator\secret.txt` 会进入 prompt/contract args；实现后相关 spec/prompt/integrity 测试 `65 passed`，`python -m ruff check core\spec_synthesizer.py tests\test_spec_synthesizer_parsing.py` 通过，全量 `.venv` pytest 为 `704 passed in 9.93s`。
- 2026-05-16 Python runtime smoke 现在会在 implementation metadata 中记录 aggregate-only 覆盖信息：`generation_metadata["runtime_smoke"]` 只包含实际执行的 case 数、contract case 数和输入维度（args/stdin/input_files/env_vars/default），不保存 argv 值、文件内容、stdout/stderr、holdout failure 或 official hidden detail。这让后续审计能区分“有本地 probe 轴”和“生成后确实跑过对应输入形态”，仍符合 cleanroom/反过拟合边界。
- 2026-05-16 本轮 runtime-smoke metadata 验证：新增 TDD 测试先失败于缺少 `generation_metadata["runtime_smoke"]`；实现后相关 implementer/integrity 测试 `55 passed`，`python -m ruff check core\codebase\runtime_smoke.py core\implementer_agent.py tests\test_agent_output_parsing.py` 通过，全量 `.venv` pytest 为 `705 passed in 10.05s`。严格 official-eligible JSON gate 仍为 `row_count=0`、`total_row_count=0`，未触发外部 LLM、Docker 或官方评测。
- 2026-05-16 `rank_programbench_candidates.py` 新增可选 `--require-runtime-smoke-dimensions` gate：可要求 official-eligible candidate 的 `implementation_metadata.runtime_smoke.status` 为 `passed`，且实际跑过指定输入维度，如 `args,input_files,env_vars`。缺维度会标记 `insufficient_runtime_smoke_dimensions`，smoke 未通过会标记 `runtime_smoke_not_passed`；默认关闭，且只读 aggregate metadata。
- 2026-05-16 本轮 runtime-smoke dimension gate 验证：新增 TDD 测试先失败于缺少 `required_runtime_smoke_dimensions` 参数；实现后 `tests/test_rank_programbench_candidates.py` 为 `58 passed`，`python -m ruff check scripts\rank_programbench_candidates.py tests\test_rank_programbench_candidates.py` 通过；真实 strict gate 加 `--require-runtime-smoke-dimensions args,input_files` 仍为 `row_count=0`、`total_row_count=0`；全量 `.venv` pytest 为 `706 passed in 10.17s`。未触发外部 LLM、Docker 或官方评测。
- 2026-05-16 `plan_official_breakthrough_targets.py` 新增 `--baseline-upgrade-require-runtime-smoke-dimensions`，只把 runtime-smoke 输入维度要求传入 ready-baseline 的 `rank_programbench_candidates.py` next command，让 planner dry-run 队列和最终本地 official-eligible gate 保持一致。
- 2026-05-16 本轮 planner pass-through 验证：新增测试先失败于 argparse 不识别该 flag；实现后 `tests/test_plan_official_breakthrough_targets.py` 为 `8 passed`，`python -m ruff check scripts\plan_official_breakthrough_targets.py tests\test_plan_official_breakthrough_targets.py` 通过；真实 planner JSON dry-run 加 `--baseline-upgrade-require-runtime-smoke-dimensions args,input_files` 输出 `row_count=12`、`total_row_count=12`，ready-baseline next command 包含 `--require-runtime-smoke-dimensions 'args,input_files'`；全量 `.venv` pytest 为 `706 passed in 10.33s`。未触发外部 LLM、Docker 或官方评测。
- 2026-05-16 packaging/closed-loop 本地 gate 现在也能要求 runtime-smoke 输入维度：`SubmissionHoldoutGate`、`package_submission.py`、`run_official_closed_loop.py` 接入同一个 aggregate `--require-runtime-smoke-dimensions args,input_files` 语义；direct closed-loop 会在 packaging/official eval 前检查 `runtime_smoke.status == "passed"` 和所需维度。
- 2026-05-16 本轮 closed-loop gate 验证：新增测试先失败于 `SubmissionHoldoutGate` 构造参数缺失、`package_submission.py`/`run_official_closed_loop.py` 不识别 flag、child command 未透传；实现后新增 focused `5 passed`，相关 packaging/closed-loop/wrapper 测试 `173 passed`，`python -m ruff check` 通过 touched Python 文件；真实 dry-run 验证 strategy ablation、weak-task、missing-holdout、restore-batch JSON、weak-batch JSON 命令均包含 `--require-runtime-smoke-dimensions args,input_files`。全量 `.venv` pytest 为 `715 passed in 10.74s`，strict official-eligible JSON gate 仍为 `row_count=0`、`total_row_count=0`。未触发外部 LLM、Docker 或官方评测。
- 2026-05-16 尝试执行 `sharkdp__hexyl.2e26437` 的 external-LLM weak-task cleanroom rerun（仍跳过官方评测）被环境策略拒绝：原因是会把 task/code context 发送到外部 LLM 服务。不要绕过该拒绝；后续除非策略允许更安全的外部执行路径，否则只能继续本地 aggregate audit、dry-run planning 和框架硬化。
- 2026-05-16 `rank_programbench_candidates.py` JSON 保留原 `official_gate` 第一个原因，同时新增 `official_gate_blockers` 列出所有 aggregate blockers，避免 runtime-smoke metadata 缺失被 low holdout / too few cases / already official 等早期原因遮住。新增 TDD 测试先失败于缺少 `official_gate_blockers`，实现后 focused `2 passed`，完整 `tests/test_rank_programbench_candidates.py` 为 `59 passed`，`python -m ruff check scripts\rank_programbench_candidates.py tests\test_rank_programbench_candidates.py` 通过；真实 latest-per-task JSON smoke 输出 `row_count=23`、`total_row_count=23`，blocker counts 包括 `runtime_smoke_not_passed=23`、`low_holdout_rate=17`、`insufficient_smoke_contract_axes=17`、`already_official=12`、`too_few_holdout_cases=10`。
- 2026-05-16 新增 `scripts\audit_runtime_smoke_replay.py`，用于审计历史 `runs/**/result.json` 候选是否能基于本地 evidence 回放 implementation runtime smoke。默认只读，`--execute` 也只运行本地生成 Python 文件，不调用外部 LLM、Docker、打包或官方评测；JSON 只输出 aggregate 状态、维度、计数和路径，不输出 argv、输入文件内容、stdout/stderr、holdout 失败或官方隐藏细节。真实 runs 下只读统计为 96 个 `insufficient_contract_artifacts`、16 个 `ready_for_replay`、4 个 `missing_entrypoint`；本地 execute 统计为 14 个 `replay_passed`、2 个 `replay_failed`、96 个 `insufficient_contract_artifacts`、4 个 `missing_entrypoint`。新增测试 `4 passed`，相关 runtime-smoke/official-gate 回归 `111 passed`，ruff passed，最新全量 `.venv` pytest 为 `722 passed in 12.29s`。这说明本地验证能力增强，但仍不能证明官方测试集突破。
- 2026-05-16 新增 `scripts\audit_runtime_smoke_gate_replay.py`，用于把 strict official gate blockers 与本地 runtime-smoke replay 结果交叉审计。真实 runs 在 `--latest-per-task --min-smoke-contract-axes 1 --require-runtime-smoke-dimensions args,input_files --require-holdout-improvement --min-holdout-improvement-delta 0.02 --execute-replay` 下得到 23 行：0 个 `metadata_only_runtime_smoke_blocker`、6 个 `replay_resolved_but_other_blockers_remain`、17 个 `replay_failed_or_incomplete`。结论是 strict official-eligible 为空不是单纯 runtime-smoke 元数据缺失造成，剩余阻断仍是低 holdout、未改善、case 不足、smoke-axis 不足或已有 official/baseline 状态。新增测试 `3 passed`，ruff passed，JSON 仍只输出 aggregate 字段。
- 2026-05-16 已确认项目 `.env` 存在并配置 `GLM_API_KEY`、`KIMI_API_KEY`，`llm_clients/factory.py` 会读取项目 `.env`。按要求尝试发起一次使用 GLM 的 `hexyl` cleanroom rerun 审批（仍带 `--skip-official-eval` 语义），但环境策略拒绝，因为会把 task/code context 发送到外部 LLM 服务。该拒绝不能绕过；真实外部 LLM 测试需要用户在本机 PowerShell 手动运行 guarded command，或切换本地 LLM endpoint/获得策略层允许。
- 2026-05-16 新增 `local_openai` LLM provider：支持本机 Ollama/LM Studio/vLLM/OpenAI-compatible `/chat/completions` endpoint，默认只允许 `localhost`/loopback URL，API key 可为空；`main.py --provider local_openai`、`llm_clients/factory.py`、`config/settings.yaml`、`config/smoke_glm.yaml` 已接入。这样外部 GLM/Kimi 出口被策略拒绝时，可以用本地 LLM 做真实 cleanroom rerun，而不把 prompt/code context 发到外部服务。
- 2026-05-21 `chmln__sd.87d1ba5` 官方 aggregate baseline 已由 no-external-LLM file_bridge `baseline_regex_patch1` 复核刷新为 counted `699/810`、score `86`，raw eval `752/869`；本地 run 为 exploration `40/40`、holdout `12/12`，runtime smoke passed。已写入 `baselines\programbench\chmln__sd.87d1ba5.baseline.json` 并同步 README/交接文档；仍只使用官方 aggregate summary，不读取 hidden 失败细节，且这只是同分 counted refinement。
- 2026-05-17 单任务官方证据审计入口已补齐：`summarize_holdout_trends.py`、`audit_runtime_smoke_replay.py`、`audit_runtime_smoke_gate_replay.py` 现在均支持 `--task <task_id>` 精确过滤。`clog-cli` / `nnn` 复核结果显示 latest official candidates 缺 `input_files` 合约，`clog-cli` 较早候选本地 replay 失败；全量 gate replay 未发现可仅靠补 runtime-smoke metadata 推进的候选。因此当前仍只有 `chmln__sd.87d1ba5` 是已证实的官方 aggregate baseline-upgrade 证据，不能把 `clog-cli` / `nnn` 视为新突破。
- 2026-05-17 `audit_runtime_smoke_gate_replay.py` 已输出 `replay_failed_issue_kind`，只给出 aggregate 失败类别（traceback/timeout/error），不输出 stderr 或文件内容。该字段能把 `insufficient_contract_artifacts` 与真实本地执行失败分开，方便下一轮定位 implementation-stage 稳定性缺口。
- 2026-05-17 `audit_runtime_smoke_gate_replay.py` 已支持 `--status` 与 `--replay-failed-issue-kind` 过滤。focused 测试 `10 passed`，ruff 通过；真实 runs 下 `metadata_only_runtime_smoke_blocker` 为 0 行，`metadata_already_sufficient` 仍只有 `chmln__sd.87d1ba5`。只读子代理同步复核严格 latest gate replay，也未发现除 `chmln__sd.87d1ba5` 外的新官方突破证据；latest implementation replay 失败在当前 Codex sandbox 下主要先表现为 executor permission denied。
- 2026-05-17 Python runtime smoke 已把候选语法错误和执行环境权限问题拆开：生成 Python 文件先做 AST syntax precheck，语法坏候选直接返回 `runtime_smoke_syntax_error`；嵌套 Python 启动被 `[WinError 5]`/permission denied 阻断时返回 `runtime_smoke_executor_permission_denied`。focused regression `tests/test_codebase_integrity.py tests/test_audit_runtime_smoke_replay.py tests/test_audit_runtime_smoke_gate_replay.py` 为 `32 passed`，ruff 通过；真实 `tarka__xcp.5e5b448` gate replay 现在筛为 `runtime_smoke_syntax_error`，`hexyl` 部分历史 replay 在当前沙箱下是 executor permission blocker，不能当作候选实现失败或官方突破证据。
- 2026-05-17 `audit_runtime_smoke_gate_replay.py` 新增 `replay_environment_blocked` 状态：`runtime_smoke_executor_permission_denied` 不再混入 `replay_failed_or_incomplete`。focused `tests/test_audit_runtime_smoke_gate_replay.py` 为 `11 passed`，ruff 通过；真实 `sharkdp__hexyl.2e26437 --latest-per-task` 可用 `--status replay_environment_blocked` 筛出，仍保留 `low_holdout_rate` / `runtime_smoke_not_passed` blocker。
- 2026-05-17 strict latest gate replay JSON 已补汇总计数：真实 `runs` 在 strict 参数加 `--allow-existing-official --execute-replay --format json --limit 1` 下总计 23 行，`status_counts` 为 `metadata_already_sufficient=1`、`replay_environment_blocked=4`、`replay_failed_or_incomplete=18`，`replay_failed_issue_kind_counts` 为 `runtime_smoke_executor_permission_denied=4`、`runtime_smoke_syntax_error=2`。这只是本地 aggregate/gate 证据，当前仍只有 `chmln__sd.87d1ba5` 是已证实的官方 aggregate baseline-upgrade 证据。
- 2026-05-17 direct `audit_runtime_smoke_replay.py` 也补齐 `--status`、`--failed-issue-kind` / `--replay-failed-issue-kind` 过滤和 JSON 汇总计数。focused `tests/test_audit_runtime_smoke_replay.py` 为 `8 passed`，ruff 通过；真实 `runs --require-runtime-smoke-dimensions args,input_files --execute` 汇总为 119 行，其中 `insufficient_contract_artifacts=96`、`missing_entrypoint=4`、`replay_environment_blocked=16`、`replay_failed=3`，失败类别为 `runtime_smoke_executor_permission_denied=16`、`runtime_smoke_syntax_error=3`。下一步应优先补强新候选的 contract artifact / input_files replay 覆盖，再做 xsv/htmlq restore 回退审计。
- 2026-05-17 `audit_restore_targets.py` 新增 `--task` 过滤和 JSON `regression_signal_counts` / `axis_delta_action_counts`。focused `tests/test_audit_restore_targets.py` 为 `6 passed`，ruff 通过；真实 `xsv` 单任务审计为 latest `0/16`、best `9/11`、回退 `-81.8%`，建议 `ablate_added_axis_domains:csv_table`；真实 `htmlq` 为 latest `7/16`、best `10/11`、回退 `-47.2%`，建议 `ablate_added_axis_domains:html_selector`。对应 restore-axis batch JSON dry-run 已生成 `xsv/htmlq` guarded command plan，仍未执行外部 LLM、Docker 或官方评测。
- 2026-05-17 继续定位 `xsv` latest replay 的 `runtime_smoke_syntax_error`：生成 `main.py` 在 `f = open_input(ns.input` 处截断，属于实现输出不完整而非官方隐藏行为证据。已给静态 integrity 和 runtime smoke 的语法错误 message 增加共享截断提示，retry prompt 会携带 `likely truncated generated output` / `compact complete source files`；同时只在疑似截断时把 previous output preview 改成 head+tail，让 retry 能看到文件尾部截断点。kind 不变，避免影响现有统计。验证：`tests/test_codebase_integrity.py` 为 `18 passed`，retry prompt + integrity/replay/restore slice 为 `44 passed`，ruff 通过；strict official-eligible gate 仍为 `row_count=0`、`total_row_count=0`，未调用外部 LLM、Docker 或官方评测。
- 2026-05-17 planner 的 restore-ablation dry-run command 也已接入 runtime-smoke 维度 gate：新增 `--restore-ablation-require-runtime-smoke-dimensions`，会透传为 child `run_official_strategy_ablation.py --require-runtime-smoke-dimensions args,input_files`。验证：`tests/test_plan_official_breakthrough_targets.py` 为 `8 passed`，ruff 通过；真实 `runs` 下 `htmlq/xsv` 等 restore 目标的 JSON next_command 已包含该 gate，仍只打印 dry-run command，未调用外部 LLM、Docker 或官方评测。
- 2026-05-17 weak-task recommendation dry-run command 也已接入 runtime-smoke 维度 gate：`summarize_holdout_trends.py` 新增 `--rerun-require-runtime-smoke-dimensions`，会透传为 child `run_weak_task_cleanroom_rerun.py --require-runtime-smoke-dimensions args,input_files`。验证：`tests/test_summarize_holdout_trends.py` 为 `34 passed`，ruff 通过；真实 `runs` 下 `hexyl/zoxide/pingu` weak rerun recommendations 的 JSON `guarded_command` 已包含该 gate，仍只打印 dry-run command，未调用外部 LLM、Docker 或官方评测。
- 2026-05-17 planner 剩余 weak/missing-holdout command plan 也已补齐 runtime-smoke 维度 gate：新增 `--rerun-require-runtime-smoke-dimensions` 与 `--missing-holdout-require-runtime-smoke-dimensions`，让 ready/restore/weak/missing 四类 official breakthrough queue 命令口径一致。验证：`tests/test_plan_official_breakthrough_targets.py` 为 `8 passed`，ruff 通过；真实 `runs` planner JSON smoke 同时开启四类 gate 后返回 `row_count=12`、`total_row_count=13`，四类 next_command 均包含 `--require-runtime-smoke-dimensions args,input_files`（部分因 shell quoting 显示为 `'args,input_files'`）；去掉 `--allow-existing-official` 的 strict official-eligible gate 仍为 `row_count=0`、`total_row_count=0`，未调用外部 LLM、Docker 或官方评测。
- 2026-05-17 新增 `file_bridge` LLM provider，为“官方测试集验证时不使用外部 LLM API、改由本地 Codex 子代理/人工接力”准备接口：chat 请求会落盘到 `output/file_bridge_llm/request_<id>.json`，等待同目录 `response_<id>.json` 或 `.txt`。`main.py --provider file_bridge` 已可选，wrapper 可传 `--config config/smoke_file_bridge.yaml`。验证：file_bridge/config/main 相关测试 `41 passed`，ruff 通过；当前只是本地文件桥能力，尚未执行 official eval，不代表新的官方测试集突破。
- 2026-05-17 已修正 differential validation 的执行器假阳性：`DiffReport.is_equivalent` 现在会把 `exit_code=-1` 且非 timeout 的 executor error 判为非等价，避免 Codex Windows sandbox 的 `[WinError 5]` 被原始/替代同错掩盖。验证：新增 TDD 后 `tests/test_differential_tester_backends.py` 为 `9 passed`，ruff 通过；使用 escalated 本地 `file_bridge` harness 在 `examples/mock_task` 上跑通无外部 LLM 闭环，result 为 success、exploration `8/8`、holdout `2/2`，产物位于 `output\file_bridge_smoke_20260517_harness5\mock_task\result.json`。该结果仅证明本地桥接/验证路径可用，未执行官方评测。
- 2026-05-18 工作区复核结论：不允许已有官方 baseline 时，strict official-eligible gate 仍为 `row_count=0`；允许 baseline upgrade 且要求 `--min-smoke-contract-axes 1`、`--require-runtime-smoke-dimensions args,input_files`、`--require-holdout-improvement --min-holdout-improvement-delta 0.02` 时，只有 `chmln__sd.87d1ba5` 通过本地 aggregate gate。`clog-cli`、`gron`、`htmlq` 最新 no-external-LLM `file_bridge` 产物 runtime smoke 已覆盖 `args/stdin/input_files/default`，但因未超过历史 best 被 `holdout_not_improved` 阻断，不能作为新的官方提交突破。`audit_official_eval_gate.py` 已补齐 `--require-runtime-smoke-dimensions`，单个 `result.json` 审计现在和批量 strict gate 口径一致；focused 测试 `tests/test_audit_official_eval_gate.py` 为 `30 passed`，全量 `.venv` pytest 为 `775 passed in 19.91s`。本轮未调用外部 LLM、Docker 或官方评测；ruff 在当前 `.venv`/PATH 中不可用，已用 `py_compile` 做语法检查。
- 2026-05-18 `plan_official_breakthrough_targets.py` 已把 ready-baseline 行的严格 baseline-upgrade gate 显式输出到 Markdown `baseline gate` 列和 JSON `baseline_upgrade_gate` 字段，复用 `rank_programbench_candidates.py` 的 aggregate-only 门禁逻辑。真实 `runs` 下开启 strict baseline flags 后，`clog-cli`、`gron`、`htmlq` 直接显示 `holdout_not_improved`，`nnn` 显示 smoke/runtime/prior blockers，只有 `chmln__sd.87d1ba5` 显示 `eligible_baseline_upgrade`。验证：`py_compile` 通过，`tests/test_plan_official_breakthrough_targets.py` 为 `9 passed`，全量 `.venv` pytest 为 `776 passed in 18.80s`；未调用外部 LLM、Docker 或官方评测。
- 2026-05-15 holdout improvement gate 已补齐公平 history 排除机制：`audit_holdout_improvement.py --exclude-root`、`run_official_closed_loop.py --holdout-history-exclude-root`、strategy ablation 自动把父 `--runs` 传给 child，避免同一批 restore-axis variants 互相污染 previous-best 判断。已用 `htmlq` dry-run smoke 复核 child 命令包含 `--holdout-history-exclude-root runs\restore_axis_ablation_dryrun\htmlq`，未执行外部流程。
- 2026-05-15 已批量执行 7 个 restore 目标的 strategy ablation dry-run：`htmlq`、`go-mod-outdated`、`gron`、`zip-password-finder`、`xsv`、`csview`、`cmatrix` 均 exit 0。每个目标输出 `baseline_no_adaptive/adaptive_profile/adaptive_deep` 三个 child closed-loop 命令，且都包含 `--skip-official-eval`、`--require-holdout-improvement`、`--holdout-history-exclude-root runs\restore_axis_ablation_dryrun\<task>`、`--max-generalization-risk low`、`--min-smoke-contract-axes 1`。
- 2026-05-26 `alecthomas__chroma.8d04def` 官方 aggregate 分数已由 3 提升到 13（65/515 counted；raw eval 73/531），来自无外部 LLM 的 file_bridge syntax-highlighter restore/generalization patch（`restore_patch3_generalization_probe`）。本地 exploration/holdout 在 40-case min-holdout gate 下均为 100%；早期 `restore_patch2` 本地 100% 但官方 0（0/515），patch3 generalization 探针是首个 chroma 官方升级，`fully_resolved=false`，仍不是 solved task。已新增通用 `syntax_highlighter` strategy pack（`core/profiling/rules/12_syntax_highlighter.yaml`）与 adaptive 探针，并把官方 eval operational-failure 细分为 `results_read_failed`/`invalid_aggregate`/`failed_without_eval_json`。patch3 源码可从 `output/file_bridge_manual/run_chroma_file_bridge.py` 完整复现（与 submission 字节一致，仅行尾差异）；全程只使用官方 aggregate，不读取 hidden 失败细节。
- 2026-05-26 在框架内开设子代理（无外部 LLM）做 cleanroom 差分探针，发现并修复一个真实路径显示 bug：`display_path` 改用 `posixpath.normpath`，使 `executable .` 报 `/rebuilder-work`（与参考一致）而非 `/rebuilder-work/.`，并泛化到 `.`/`..`/冗余分隔符/尾随斜杠。新增变体 `restore_patch4_generalization_probe`（建于 patch3 之上，patch3 输出字节不变）。本地差分 vs cleanroom 参考由 72/81 升到 73/81，4 个新路径边界探针全匹配，无回归。官方 eval 复核为 raw 73/531、counted 65/515、score 13，与 patch3 **完全相同**（官方测试集未覆盖目录/路径边界用例），因此 patch4 是经官方验证的**本地正确性改进但官方中性**，不更新 baseline。结论：chroma 剩余官方差异主要在 per-style ANSI 颜色表与完整 `--list` catalog，受反过拟合/static-asset 政策约束不应硬编码；当前合规改进上限已达，官方突破仍为 score 13。
- 2026-05-26 `burntsushi__xsv.f430466` 官方 aggregate 分数已由 50 提升到 **70**（counted 832/1186；raw eval 953/1317；error_code None，13 个 branch 全部运行），来自无外部 LLM 的框架内子代理 file_bridge `restore_patch8`。该 patch 从黑盒差分探针发现并以**通用算法**补全/重写了多个子命令（stats 的 mode/median/cardinality/nulls/select/no-headers + Welford 方差——patch7 在 `stats -s 列` 直接崩溃；flatten 多记录流式；fmt 的 --crlf/--out-delimiter/--quote-always/--ascii；count/reverse/sort/search/slice/table 长选项；headers --just-names/--intersect；split --size/outdir；input quoting；usage 错误串），并修复了 `split --size 0` 的无限循环（匹配参考 `--size must be greater than 0.`）。本地 cleanroom 差分由 86/151 提升到 143/151（+57，0 回归），336 组合 hang 猎杀 0 挂起，5000 行无性能回归；剩余未匹配项（Rust HashMap 哈希序、RNG）按反过拟合政策**不硬编码**。这是本轮**更大的官方突破**（+20 score、+242 counted、+282 raw），`fully_resolved=false`，仍不是 solved task。排障记录：官方 eval 曾因 24 个累积孤儿容器、split 死循环、以及中文 Windows GBK 无法打印 ✅ emoji 三个操作性问题反复失败，已逐一解决（清容器 / 修 hang / `PYTHONUTF8=1`），最终干净完成。

## 二、接下来应该做什么
1. 继续批量推进剩余 ProgramBench 任务，优先用 stdin 修复、coverage-aware probe、runtime smoke gate 和 task profile strategy 后的框架重跑候选，再决定是否进入官方 eval。
2. 继续用 domain strategy pack 复跑 pingu/htmlq/gron/xsv/go-mod 等代表任务，优先看 runtime smoke gate 与 profile prompt budget 是否改善实现阶段失败和本地 gate；仍不使用官方失败细节。
3. 每次官方分数有突破时，及时记录基线并更新 README。
4. 保持 runner 幂等性和 LLM/Docker 连通性监控，确保批量实验稳定。
4. 继续做带 improvement gate 的 assets on/off ablation，并把新增 output directory probing 和 phase-level usage metadata 带进实验观察。
5. 若遇 workspace 或评测异常，优先自动修复并复跑，必要时切换下一个候选。
6. 定期整理和归档突破进展，便于后续交接和复盘。

---
文档更新时间：2026-05-26

